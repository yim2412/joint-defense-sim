# 0단계 — 지형 · 사각 지도 · 탐색 품질 캘리브레이션

> **기준 커밋**: `daa7ce8` (2026-09-20) · 인용은 전부 이 시점 기준.
> 규약 2.0. 이 판은 **분석하지 않고, 무엇을 분석할지 정한다.**

---

## (a) 지형 `[실측]`

```
$ git -c core.quotepath=false ls-files   -> 추적 282개
.py 71개 · 40,493라인   |   .md 60개 · 15,311라인
```

| 분류 | 파일 | 라인 | 비고 |
|------|-----:|-----:|------|
| 런타임-엔진 (`engine_*`·`db_*`·`forecast_features`·`scenarios`) | 13 | **20,388** | 전체 코드의 **50%** |
| 런타임-UI (`app_*`·`ui_*`·`mixin_*`) | 18 | 11,907 | |
| 감사도구 (`audit_*`·`_audit_*`·`check_protocol`) | 21 | 5,154 | |
| 보조도구 (`_build`·`_bg`·`improve_*` 등) | 11 | 1,890 | |
| RL·학습 (`ai_*`·`_ai_*`) | 8 | 1,154 | |
| 분석 산출물 (`analysis/`) | 2 | 897 | 이번 판에서 생김 |
| 문서 | 58 | 14,430 | |
| 자산 (jpg·webp·png·pdf·npz) | 132 | — | |

**분석 규모 기준선 = 런타임 코드 32,295라인**(엔진 20,388 + UI 11,907).
도구·문서·학습 코드는 3단계·4단계에서 별도로 본다.

### 편중

```
engine_combat.py   8,899라인  = 전체 .py 의 22%
db_specsheet.py    3,517
engine_core.py     2,574
```

`engine_combat.py` 한 파일이 **엔진 절반**이다. 2단계(모델 타당성)의 무게는
사실상 이 파일에 걸린다 — 판을 짤 때 이 파일을 통으로 읽는 판 하나가 필요하다.

### 문서 표와 실제의 불일치

- **`CLAUDE.md` 파일 구조 표: 루트 `.py` 70개 중 30개만 등재 — 40개 누락** → **F-001**
- README는 `chk_readme_coverage` 가 전수 커버를 강제해 불일치 0. **한쪽만 도구가 있다.**
- 루트 `.py` 70개 중 **다른 `.py` 가 참조하지 않는 것 30개**. 대부분 `main()` 을 가진
  독립 실행 도구라 정상이지만, 하나는 진짜로 죽어 있다:
  **`db_ground_threat.py`(415줄) — 어디서도 import 되지 않는다** → **F-002**

---

## (b) 사각 지도 `[실측]`

**기존 도구가 무엇을 보는가**

| 도구 | 오라클 | 자동 실행 |
|------|--------|-----------|
| `audit_static_scan.py` (검사 **24함수** / 출력 56항목) | 구조 정합 | pre-commit |
| `audit_verify_regression.py` (42케이스 × 32지표) | 자기참조 골든 | pre-commit |
| `analysis/check_protocol.py --stage b` | 분석 규약 | pre-commit |
| `audit_property.py` | 독립 불변식 | pre-push |
| `audit_effect.py` | 토글 효과 | pre-push |
| `_audit_roundtrip` · `_audit_ui_shot` · `_audit_render_smoke` | 실행·시각 | pre-push |

**그런데 감사 도구는 20개다 — 훅이 자동으로 돌리는 것은 8개뿐이다.**

```
수동 실행 13개: _audit_campaign_smoke, _audit_compat, _audit_gui_smoke, _audit_load_guard,
  _audit_make_pdf, _audit_mc_stability, _audit_scenario_smoke, _audit_smoke_util,
  audit_db_consistency, audit_dead_toggle, audit_fuzz, audit_pairwise, audit_perf
```

`BLIND_SPOTS.md` 가 **"메웠다"고 선언한** 사각 중 둘(`audit_pairwise` 의 pairwise 조합,
`audit_fuzz` 의 수치 키 극단값)이 **바로 이 수동 목록에 있다.** 종합 감사는 규정상
**major 전환 때만**(CLAUDE.md 감사 3층) 도므로, 실질적으로 **major 당 1회** 돈다.

> **"메웠다"와 "돌고 있다"는 다르다.** 도구가 존재하는 것과 실행되는 것을 구분하지
> 않으면 사각 레지스트리가 낙관적으로 거짓말을 한다. → **F-003**

**표본 검증 `[실측]`** — 도구가 *주장*하는 것을 실제로 잡는지, 결함을 주입해 확인했다.
(규약 2.0b: 표본에서 하나라도 새면 그 영역은 전수로 전환한다.)

| 주입한 결함 | 결과 |
|-------------|------|
| README의 적군 DB 수치 `70종` → `999종` | ✅ `[FAIL] ⑥ README DB 항목수·단계 버전 = 실제 — 적군 README=999≠실제=70` |
| `mixin_configpanel.py` 에서 `enable_ship_evasion` cfg 빌드 줄 삭제 | ✅ `[FAIL] ① 3종세트 enable_ship_evasion` + `[FAIL] ① 숨은 플래그 없음` (2중 검출) |
| `ui_monitor.py` 에 `from app_utils import _GLOBAL_POOL` 주입(3라운드) | ✅ `[FAIL] ① 재할당 전역 이름 import 금지` |

**3/3 검출 · 샌 것 0.** 복구 후 56/56 PASS 확인. → 정적 스캔이 커버한다고 주장하는
영역은 **전수 재훑기 대상에서 제외**하고, 3단계에서 *커버 범위 자체*만 따진다.

**의존 구조 `[실측]`** — 모듈 70개, **순환 0**. `app_main` 을 import 하는 하위 모듈은
감사 도구 3개뿐(`_audit_render_smoke`·`_audit_roundtrip`·`_audit_ui_shot`)이고
이들은 계층 밖이라 위반이 아니다. **1단계 뼈대는 건강하다** — 판을 얕게 잡아도 된다.

**무거운 도구 실행** — `audit_pairwise` 를 이 판에서 직접 돌렸다(아래 결과).

---

## (c) 탐색 품질 캘리브레이션 `[실측]`

> 규약 2.0(c): **정지 규칙(5절)을 적용할 자격**이 여기서 나온다.
> 방법이 과거 결함을 못 잡으면 "발견 0건"은 성과가 아니라 무능의 증거다.

### C-1. 9영역 × A단계 전수 대조 — **방법 자체의 구멍을 먼저 쟀다**

`CLAUDE.md` 종합 감사 9영역의 개별 점검 항목을 A(2.0~2.5)와 1:1로 대조했다.
6라운드에서 ①영역만 했고 나머지는 안 했는데, **②③④⑤에서 7건이 더 빠져 있었다**:

| 영역 | A에 없던 것 | 이게 없어서 놓쳤을 실제 사건 |
|------|-------------|------------------------------|
| ② | **DB 테이블 간 키 정합** | 054B형이 `ENEMY_SHIP_SAM_LOADOUT` 에 없어 SAM·CIWS **0발** → *"054A보다 강한 발전형"* 의 정체성 역전 |
| ② | **재고·편성 규모 현실성** | *'포화'인데 6발* 프리셋(`7c04561`) · 항모 함재기 무한 생산(v22 1순위) |
| ② | `normalize_enemy_db` 누락 필드 | — |
| ③ | **결정론**(신규 `random` 이 RNG 순서를 깨는가) | 골든이 흔들리는 원인 |
| ④ | **wall-time 회귀 가드** | 성능 급증 · *기준선 자체가 stale* 인 경우 포함 |
| ⑤ | **abort 후 워커 잔존 · 외부 의존(CDN)** | 정상 경로에서는 안 보이는 자리 |

전부 2.2·2.3·2.4에 반영했다(커밋 참조). **A를 두 번 보강한 셈**이고, 두 번 다
원인은 같다 — *백지에서 설계하면 이미 아는 것보다 좁아진다.*

### C-2. 과거 결함 재현 — 이 판에서 실제로 잡은 것

| 과거 실제 사건 | 이 판의 방법이 잡았나 |
|----------------|----------------------|
| `CLAUDE.md` 파일표 누락(v17.01.09에 `engine_campaign` 1건으로 한 번 터졌던 부류) | ✅ **재발 발견** — 이번엔 40개 규모(F-001) |
| 죽은 코드(호출처 0) | ✅ `db_ground_threat.py` 발견(F-002) |
| "메운 사각"이 실제로는 안 도는 문제 | ✅ 신규 발견(F-003) |
| 재할당 전역 이름 import | ✅ 이미 도구화됨(`chk_global_name_import`, 3라운드) |

**판정**: 0단계의 방법(전수 스크립트 + 문서-실제 대조 + 도구 실행 추적)은
과거 부류를 **재발까지 포함해** 잡아냈다. 정지 규칙을 적용할 자격은 생겼다.
다만 **이것은 0단계 방법의 검출력**이지, 2단계(모델 타당성)의 검출력은 아니다 —
그건 2단계 판에서 따로 재야 한다.

---

## (d) 이 판의 결론 — 1~4단계를 어떻게 짤 것인가

0단계는 *무엇을 분석할지 정하는 판*이므로, 결론은 **판 구성의 변경**이다.

| 단계 | 0단계 실측이 시사하는 것 | 조정 |
|------|--------------------------|------|
| 1 뼈대 | 모듈 70개 **순환 0**, 단방향 유지, 재할당 전역은 이미 도구화 | **얕게.** 2.1의 6~9(전역 오염·짝 지도·순회 필터·cfg 키)만 본다 |
| 2 모델 타당성 | `engine_combat.py` 8,899줄 중 **`_compile()` 하나가 1,802줄**이고 32지표 정의가 전부 거기 있다. F-005 가 이미 나왔다 | **가장 두껍게. 판을 둘로 나눈다** — 2-A 물리·확률 / **2-B `_compile()` 지표 정의 전용** |
| 3 검증 체계 | 표본 3/3 검출 — 도구는 주장대로 잡는다. 문제는 **범위와 실행 주기**(F-003) | 방향 전환: *"도구가 맞나"* 가 아니라 **"무엇을 안 보나 · 언제 도나"** |
| 4 부채·성능 | `db_*` 미배선 13건(L-2) + 죽은 모듈 1건(F-002). 성능은 미측정 | 죽은 코드는 **스크립트 + 표본 판정 2단**으로(거짓양성 87건 실측) |

**우선순위 초안** (4.1 절차 — 의존 → 묶음 → 심각도 → 회귀위험):

1. **F-005 + F-004** — 같은 함수(`_compile`)·같은 축(지표 정의). 2-B 판에서 함께.
   [[project-fleet-metric-flaw]](v22 1순위)와 **같은 뿌리**라 로드맵과 충돌하지 않는다.
2. **F-001 + L-1** — `CLAUDE.md` 표·수치. 같은 파일·같은 뿌리(검사 부재) → 한 묶음.
   S형식(`chk_claude_md_coverage`)이라 영구 봉인된다.
3. **F-003** — 훅 배선. 가벼운 도구부터.
4. **F-002 + L-2** — `db_*` 배선 vs 삭제. **사용자 결정 필요**(4.4 일괄 제시 대상).

> **4.2 회피 경보 자체 점검**: 상위 4개 중 **1·4가 구조적**(지표 정의 축·데이터 배선)이고
> 2·3은 도구·문서다. *"상위 5개 중 최소 2개는 구조적"* 기준을 만족한다.

## (e) 이 판에서 확인한 내 방법의 한계

- **0단계 방법(전수 스크립트·문서 대조·도구 추적)의 검출력만 쟀다.** 2단계의 검출력은
  아직 모른다 — `_compile()` 1,802줄을 읽어 물리·확률을 검산하는 일은 성격이 다르다.
  2단계 착수 시 **그 판 전용 캘리브레이션**을 다시 해야 한다.
- **거짓 양성이 실제로 87건 나왔다.** 스크립트 결과를 그대로 대장에 올렸으면 대장이
  쓰레기통이 됐을 것이다(3.1의 경고 그대로).
- `audit_pairwise` 실행 결과는 이 판 종료 시점에 별도로 붙인다.
