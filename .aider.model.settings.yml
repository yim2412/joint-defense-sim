# NOTE: ASCII only. aider reads this file with the system codec (cp949 on this PC),
# so any non-ASCII byte (Korean text, em dash) breaks loading and num_ctx is
# silently ignored -- see plan_local_llm.md section 1-c.

- name: ollama/qwen2.5-coder:7b
  edit_format: diff
  use_repo_map: true
  extra_params:
    num_ctx: 32768

- name: ollama/qwen2.5-coder:14b
  edit_format: diff
  use_repo_map: true
  extra_params:
    num_ctx: 32768

# MoE 30B-A3B (qwen3moe), native 262144. Best local model measured on this PC.
# Kept at 32768 on purpose: 262144 DOES fit app_main.py (238k), but prefill then
# takes >10min and aider's 600s HTTP timeout kills the request at 99%.
# See plan_local_llm.md section 1-d before raising this.
- name: ollama/qwen3-coder:30b
  edit_format: diff
  use_repo_map: true
  extra_params:
    num_ctx: 32768
