# 로드맵 정본 — v22

> **이 파일이 살아 있는 계획의 유일한 자리다.** (2026-09-19 축 재정립)
>
> **작성 규칙**(전역 CLAUDE.md 5번):
> - **얹지 말고 고친다.** 세션마다 앞에 새 블록을 쌓지 않는다. 끝난 줄은 **지운다** —
>   근거는 changelog·감사보고서·SESSION_LOG 에 이미 있다.
> - 날짜별 스냅샷이 필요하면 이 파일이 아니라 `SESSION_LOG.md` 에.
> - `patch_queue` 메모리는 **이 파일의 요약 포인터**로만 유지한다.

---

## 현재 위치 (2026-09-19)

```
v11 ─ v12 ─ v15 ─ v16 ─ v17 ─ v18 ─ v19 ─ v20 ─ v21   ✅ 완주
기본   물리   AI   도메인  군수   해군   공군   육군   합동
                          캠페인
                                                  ↓
                                            v22 = 이 문서
```

`APP_VERSION v21.06.05` · 워킹트리 클린 · 정적 54/54 PASS · 회귀 38×29 PASS.
v21 블록 종합 9영역 감사 통과(2026-07-24). GitHub Release draft 준비 완료·공개 보류.

---

## 축 — 트레이드 스터디 도구 (2026-09-19 확정)

> ### **"이 위협에 대해, 어떤 편성이 요구조건을 충족하는가 — 얼마에."**

`plan_roadmap.md`(2026-09-12)의 *"게임이 아닌 분석 도구"* 선언을 **실제로 구현된 기능
구성에 맞춰 한 단계 좁힌 것**이다. REQ 요구조건 판정 · 비용 대비 효과 · 적정 편대 추천 ·
Sobol 민감도 · 스트레스 테스트는 전부 **체계 설계 의사결정 지원(트레이드 스터디)** 의
표준 구성이고, 폴더명 `국방시스템공학` 과도 맞는다.

### 판정 규칙 — 모든 기능·화면·수정에 이것 하나를 묻는다

> **"이게 『편성 A와 B 중 무엇을 고를 것인가』에 답하는 데 기여하는가?"**

| 항목 | 판정 | 처리 |
|---|:--:|---|
| 교환비 · 방어 포화도 · 요격당 비용 · 탄약 효율 | ✅ | **1면으로 승격** |
| 요격률 (편성 간 분모 미통제) | ❌ | 게이지·REQ-01·등급 배너에서 **강등** |
| MC 기본 1.1시간 · 정밀 모드 14시간 | ❌ | **반복 질의 불가 = 치명적.** 축소·제거 |
| BMD 5계층 · 합동 화력 (기본 OFF) | ✅ 기여해야 함 | **기본 ON 승격 판단** |
| 3D 전장 | ❌ | 유지하되 **별도 트랙** |
| AI/RL · self-play | ❌ | **동결** |
| 감사 인프라 (정적·골든·property·effect) | ✅ 토대 | 유지·강화 |

**이 선택의 비용(명시):** ① 3D 개선이 후순위로 내려간다 ② 공군·육군·합동 층은
캠페인 모드 전용이라 **단발 편성 트레이드 스터디에 직접 기여하지 않는다**. 삭제가 아니라
당분간 우선순위를 못 받는다는 뜻이다. 이 비용을 알고 택했다.

---

## 근거 — 2026-09-19 실측

축을 바꾼 이유는 취향이 아니라 아래를 실제로 재서다. 전부 이 PC에서 직접 실행한 값이다.

### 1. 헤드라인 지표가 편성 비교를 못 한다 (MC 25회, 적=랴오닝 항모전단 고정)

| 아군 편성 | 함정 | 총 위협 | 교전시간 | MC 요격률 |
|---|---:|---:|---:|---:|
| 단독 작전 | 1척 | 112발 | 1,923s | **21.7%** |
| 이지스 기동전단 | 6척 | 166발 | 2,420s | **16.1%** |
| 전 이지스 기동전단 | 7척 | 181발 | 2,565s | **12.5%** |

편성을 7배로 키우면 요격률이 절반 아래로 떨어진다. 큰 편성이 오래 버티고 그동안 적이
더 쏜다 — **분모가 통제되지 않는다**(`docs/analysis/01`). `wing_cap` 승격으로 438→181발로
줄었지만 **구조는 그대로다.**

`recommend_fleet_v7`은 v21.06.04에서 고쳤다. **REQ-01·요격률 게이지·등급 배너는 안 고쳤다.**

### 2. 등급 배너가 실측과 반대 처방을 낸다

> 🔴 미흡 — … **"편대 규모·요격 무기 재고를 재검토하세요"** / 🟡 양호 — … **"함정 수를 보강하면 향상 여지가 있습니다"**

위 표대로 **함정 수를 늘리면 이 숫자는 떨어진다.** 반면 24번째 페이지 '전황 지표판'의
자동 해석은 *"채널 포화 — 추가 함정 편입 또는 CEC 활성화로 채널을 분산하세요"* 로 정확하다.
**틀린 진단이 1면에, 맞는 진단이 뒤에 있다.**

### 3. 기본 실행이 1.1시간 (단발 1회 1.69초 실측 기반, 8코어 병렬 가정)

| 모드 | MC | LHS | 스트레스(12셀) | 총 시뮬 | 소요 |
|---|---:|---:|---:|---:|---:|
| ⚡ 빠름 | 5,000 | 1,000 | 3,600 | 9,600 | 약 36분 |
| 📊 **표준(기본값)** | 10,000 | 2,000 | 6,000 | **18,000** | **약 1.1시간** |
| 🔬 정밀 | 100,000 | 10,000 | 36,000 | 146,000 | 약 8.6시간 |
| 정밀 전용 Sobol | — | — | — | +98,304 | +5.8시간 |

조기 수렴 종료 없음(`ConvergenceWidget`은 표시 전용). 요격률 표준편차 ±3.6%p 기준
**500~1,000회면 충분한데 10,000회를 돈다.** 트레이드 스터디는 반복 질의가 본질인데
1회 1시간이면 성립하지 않는다 — 실제로 `docs/analysis/01`도 MC 60회로 수행했다.

### 4. 엔진이 계산한 값이 화면까지 도달하지 않는다

| 위협 추적 컬럼 | 실측 | 원인 |
|---|---|---|
| 유형 | **60/60 전부 `?`** | `ev.enemy_info`가 **빈 dict** (ENEMY_DB 70종은 전부 `type` 보유) |
| 탐지거리(km) | **60/60 전부 `0`** | `ev.detect_m` = 0.0 |
| 격추거리(km) | **기본 설정에서 75% 공백** | PNG OFF: 요격 28건 중 7건(25%) / PNG ON: 33건 중 33건(100%) |

**격추거리는 다층 방어의 핵심 지표**(얼마나 멀리서 막았는가)인데 기본 설정에서 4건 중
3건이 빈칸이다. PNG는 *"검증 완료·레이블 stale·즉시 승격 가능"* 으로 방치돼 있다 —
**한 줄 승격으로 결과창 컬럼이 살아난다.**

### 5. 기본 설정이 v7 시절 모델이다

하위 호환 규칙(*"기본값 OFF — 기존 결과와 동일"*)을 21개 메이저 동안 적용하고 승격을
거의 안 한 누적 결과:

- 체크박스 54개 중 **24개(44%)가 "(실험적)"** — README가 헤드라인으로 광고하는
  **BMD 5계층은 전부 기본 OFF**, 그중 3개는 "(실험적)"
- 신규 실행 초기 체크 ON 15 / OFF 36 · 저장본 복원 기본값 ON 8 / OFF 40
  (→ 같은 설정이 열린 경로에 따라 다른 물리로 돈다)
- MC 차트·Excel/PDF 보고서 제목이 **`이지스 기동전단 통합 방어 시뮬레이터 v7.0`**

### 6. 시나리오 난이도 라벨이 실제와 역전돼 있다

| 프리셋 | 편성 | 발생 위협 |
|---|---|---:|
| **입체 포화 (최강)** | J-20×2 + DF-17×1 + 055×1 + 093×1 = 5 플랫폼 | **38발** |
| 랴오닝 항모전단 | 7 플랫폼 | **181발** |

"최강"이 4.8배 약하다. `전 이지스 기동전단 vs 입체 포화(최강)` = 요격률 99.8% · REQ 7/7
만점 · 손실 0. **DF-17 ×1 편성은 CLAUDE.md 감사 규칙 ②가 직접 경고하는 패턴**이고,
이 프리셋은 **회귀 골든의 고정 케이스**(`입체포화-이지스`)이기도 하다.

### 7. 작은 글씨에 판단 근거가 들어 있다

색 대비는 **전부 WCAG AA 통과**(C_TEXT 14.6:1 · C_SUBTEXT 4.64:1). 문제는 크기다.
9px×1 · 10px×7 · 11px×30. 10px가 쓰인 곳은 장식이 아니라 **전황 지표판 카드 부제
(계산 근거) · 이전 실행 대비 변화량(델타) · 쇼케이스 예상 효과 수치**다.
**가장 큰 글씨(26pt)가 결과값이고 가장 작은 글씨(10px)가 그 근거다 — 정반대다.**

---

## 묶음 C — 회귀 무관 (즉시 착수)

**표시 계층·UI 기본값·위생만 건드린다. 골든·엔진 무영향 → 회귀 위험 0.**
설계 합의 불필요(전역 규칙 2번 '낮음 난이도').

### C-1. 명백한 표시 오류
- `mixin_configpanel.py:792` — `QLabel("C&&D …")` → 화면에 **`C&&D`** 그대로 (같은 카드
  제목 `QGroupBox`는 `&&`가 정상이라 표기가 엇갈린다). QLabel에서 `&&`→`&`
- `mixin_configpanel.py:1283,1301` — **"승리당 승리 없음"** 문장 깨짐
- `engine_combat.py:8168` — 차트·보고서 제목 **`이지스 기동전단 … v7.0`** → 현재 이름·`APP_VERSION`
- `app_launcher.py` 스플래시 부제 **"한국 해군 이지스 기동전단 다층 방어 시뮬레이터"** → 합동 확장 반영
- 탑재 기능 탭 **"전술 요구조건 8가지"** → 실제 **7항목**(REQ-03 결번).
  코드 주석("8항목")·docstring("7항목")·UI("8가지") 3중 불일치 통일

### C-2. 엔진 값 → 화면 연결
- `_build_active_events`: `enemy_info` 실제 전달 → **유형 컬럼 부활**
- `detect_m` 기록 경로 복구 → **탐지거리 컬럼 부활**
- `_update_cards`: `f"{x}회" if x else "—"` → **0과 N/A 구분**(0회는 "0회"로)

### C-3. 가독성
- **10px·11px → 12px 일괄 상향**(`mixin_resultpanel.py:143,510,1135,1141` ·
  `mixin_configpanel.py:715` · `mixin_configpanel3.py:295` · `mixin_showcase.py:210`)
- `app_theme.py:39` **미체크 = 빨강 → 회색**. 체크박스 54개 중 36개가 기본 미체크라
  **첫 화면 글자의 2/3가 경고색**이다. ON/OFF는 이미 인디케이터 채움이 구분하고,
  녹색 채움 + 빨간 글씨는 적록 색각에 최악 조합이다. 빨강은 실제 경고에만 남긴다
- 텍스트 가로 잘림(스크롤바가 글자를 덮음) · 기본 창 1800×1060에서
  **"시나리오 저장/불러오기" 버튼 세로 잘림** → 열 재배치
  (해역 4개·항공 자산 8개 열은 60%+ 공백, 환경·방어전술 열은 잘릴 만큼 과밀)

### C-4. UI 기본값 (엔진 무관)
- **MC 기본 10,000 → 1,000 + 수렴 조기종료** → **1.1시간 → 약 6분**
- **정밀 모드(100,000회) 제거** 또는 현실적 수치로

### C-5. 제거·위생
- **`db_ground_threat.py`(415줄)** — import 참조 **0**인데 `app_main.spec` `datas`에
  등재돼 **exe에 번들된다**. 활용하거나 제거하거나 결정
- **`enable_anti_sam` UI 노출 제거** — `EFFECT_DEAD` 종결인데 체크 가능 상태.
  레이블 "(종결 — 발동 안 함)"이 길어 텍스트 잘림까지 유발
- **`_PLANS` 정리** — `/code-review`·`CLAUDE.md`·`self-play` 등 개발 용어 노출(exe 용어
  규칙 위반) + "상시·정책·유지 규칙" 2건은 개발 프로세스라 사용자 탭에서 제외.
  `plan_roadmap.md`(7트랙) ↔ `_PLANS`(3건) 동기화
- **문서 경로 stale** — `CLAUDE.md:403-404` · `README.md:85,310` 의
  `dist/이지스_기동전단_시뮬레이터/` → 실제 `dist/합동_통합방어_시뮬레이터/`.
  **적힌 `Copy-Item` 명령을 그대로 실행하면 엉뚱한 폴더를 만든다**
- **`CLAUDE.md` 파일 구조 표에 `mixin_configpanel2.py`·`mixin_configpanel3.py` 누락**
  (README에는 있음)
- 시작 화면 **JDS 로고 중복**(사이드바 + 본문) 정리

---

## 묶음 B — "요격률을 편성 비교에 쓰지 않는다" (골든 무관)

**원칙 1개를 3곳에 마저 적용한다.** 골든 지표 29개는 전부 엔진 stats이고 REQ·게이지·
배너는 들어 있지 않다 → **회귀 무관**. 다만 판정 기준 변경이라 `plan_*.md` 설계 후 착수.

| 대상 | 현재 | 방향 |
|---|---|---|
| **REQ-01** | `MC 평균 요격률 ≥ 95%` | 편성 비교 가능한 식으로 교체(편대 추천과 동일 계열) |
| **요격률 게이지** | 1면 헤드라인 (90/60 경계) | 강등 — **교환비·방어 포화도로 교체** |
| **등급 배너** | 잘못된 처방 | 지표가 바뀌면 처방이 따라온다. 전황 지표판 자동 해석 문구 계열로 |
| **REQ-02·07** | 7조합 전부 통과 = **변별력 0** | 임계값 재설정 또는 항목 교체 |
| REQ 임계값 전반 | 모듈 상수 하드코딩 | **분석자가 정의하는 입력**이어야 한다(시스템공학 요구조건의 본질) |

부수: 1면/2면 지표 교체 — **교환비·요격당 비용·방어 포화도·탄약 효율은 이미 구현돼 있고
분모가 통제된다.** 새로 만들 게 아니라 **자리만 바꾸면 된다.**

---

## 묶음 A — "기본 설정을 현재 모델로" (골든 1회 갱신)

**골든에 영향을 주는 것은 여기만 모은다 — 갱신 판단을 한 번만 하기 위해서다.**
`plan_*.md` 설계 후 착수. 골든 영향 범위는 **재봐야 안다**
(`wing_cap` 때 "전면 갱신"이 추측이었고 실제로는 3/38이었다).

- **PNG 기본 ON 승격** — 검증 완료·레이블 stale. 승격 시 **격추거리 컬럼 25%→100%**
- **"(실험적)" 24개 재분류** — 검증된 것은 레이블 제거, 미검증은 별도 섹션 격리.
  44%가 실험적이면 레이블이 정보를 잃는다
- **BMD 5계층 기본 ON 판단** — README 헤드라인 기능이 실행 시 0개 활성.
  기본 ON 하거나 README 표현을 "옵션"으로 하향(둘 중 하나는 해야 한다)
- **신규 실행 초기값 ↔ 저장본 복원 기본값 불일치 해소**(지형·ISA·증발덕팅·소나·침수)
- **`입체 포화 (최강)` 편성 현실화** + 프리셋 난이도 라벨 정정 → 골든 앵커 정상화
- 골든 `--update` 1회 + 종합 감사 ②DB·③회귀

---

## 별도 트랙 (축 밖 — 순서만 고정, 지금은 안 건드림)

1. **3D 전장 표현** — 사용자 직접 지적(2026-09-19). 미사일·명중점에 **`label` 없음**,
   **요격 성공과 함정 피격이 같은 노란 점**이라 구분 불가, 아군 미사일 종말 마커 없음
   (`if is_enemy:`), 범례 없음, 명중 표시 2.5초뿐, 타임라인이 가상 UTC 절대시각,
   아군 함정 라벨 1개만 표시, 설정 해역과 실제 배치 불일치.
   **최소 개선 3가지**(요격/피격 마커 색 분리 + `label` 추가 + 범례)는 `build_czml`
   필드 추가만으로 가능
2. **전력 DB 심화** — `_BASE_AIR_THREAT = 6.0` 하나가 적 공군 전체를 추상한다.
   해군은 개별 제원까지 내려갔는데 공군·육군은 스칼라라 **층별 충실도가 불균질**
3. **캠페인 층 효과 게이트 확장** — `chk_effect_coverage`가 `engine_combat.py` 소비
   플래그만 강제해 캠페인·공군·육군·합동 토글 10여 개가 자동 면제.
   **v20.5 부채 43건을 만든 구멍이 여기엔 아직 열려 있다**(BLIND_SPOTS 2번)
4. **E1 캠페인 MC 병렬화** — 분포 꼬리 정확도
5. **v16.1 대잠전 균형** — `_PLANS` 유일 미완 버전 항목. ⚠ 수치 튜닝 재시도 금지
   ([[project-asw-emcon-finding]]). **미완으로 남겨두는 것도 결정이다**
6. **UI 시각 감사 도구(`_audit_ui_shot.py`)** — `WA_DontShowOnScreen` + `widget.grab()`
   로 화면 점유 없이 전 화면 캡처 가능함을 확인(2026-09-19). QLabel `&&` 검사 ·
   `sizeHint().width()` 초과 = 텍스트 잘림 검사 · 기준 스크린샷 diff.
   **BLIND_SPOTS 5번("시각 오류는 사람 눈 몫")을 부분 해소할 수 있다.**
   ⚠ **단, C를 끝낸 뒤에 만든다** — 지금 고칠 코드에 회귀 가드를 먼저 만드는 건 순서가 거꾸로

---

## 미채택 / 트리거 대기 — 다시 논의하지 않기 위해 명시

| 항목 | 상태 |
|---|---|
| **AI/RL · self-play 트랙** | **동결**(2026-09-19). 기본 OFF·실험적·"이미 확인된 천장". 축 밖 |
| 릴리스 exe 공개 | **draft 준비 완료 · 공개 보류**(2026-09-12 사용자 결정). 태그 미생성 |
| 멀티플레이어 대전 | **미채택**(2026-07-24) |
| 단발 교전 모델 삭제 | **트리거 미충족 · 존속 확정** |
| 레이저(v17.2) | **음성 확정 종결** [[project-laser-efficacy-finding]] |
| 대잠 EMCON 수치 튜닝 | **막다른 길** — 구조 변경만 유효 [[project-asw-emcon-finding]] |
| 정밀화 B1(격자 촘촘화) · D1(haversine) | **제외** — 개선이 역효과 |
| "분석/시연 2모드 분리" | **미채택**(2026-09-19). 기본 설정 2개 = 검증 표면 2배. 전역 규칙 9(YAGNI) |

---

## 계획 문서 지도

| 자리 | 역할 |
|---|---|
| **`plan_roadmap.md`** (이 파일) | **살아 있는 계획 정본.** 여기만 고친다 |
| `app_launcher.py` `_PLANS` | **사용자에게 보이는** 로드맵(exe 향후 계획 탭). 용어 규칙 적용 |
| `SESSION_LOG.md` | 세션 매듭 판단 맥락(왜/다음/미커밋). 날짜별 누적 |
| `patch_queue` 메모리 | 이 파일의 **요약 포인터**. 계획 본문 중복 금지 |
| `docs/analysis/` | 도구 자체 검증 기록(01 편대 생존 · 02 편성비교 지표) |
| `_archive/plans/` | 구현 완료된 설계 문서 |
| `감사보고서.md` | 블록별 감사 결과 누적 |

---

## 착수 순서

```
1. 묶음 C  회귀 무관 — 즉시 착수 (설계 합의 불필요)
             C-1 표시 오류 → C-2 값 연결 → C-3 가독성 → C-4 UI 기본값 → C-5 제거·위생
2. 묶음 B  지표 원칙 — plan_*.md 설계 → 합의 → 구현 (골든 무관)
3. 묶음 A  기본 설정 현대화 — plan_*.md 설계 → 합의 → 구현 → 골든 1회 갱신 → 감사
4. 별도 트랙 재판단 (3D · DB 심화 · 캠페인 게이트 · UI 감사 도구)
```

**C를 먼저 두는 이유**: 회귀 위험이 0이고, 확인된 결함의 대부분이 여기 있으며,
**골든을 안 건드리므로 A·B의 설계를 기다릴 필요가 없다.** C-4(MC 기본값)만으로
실행 시간이 1.1시간 → 약 6분이 되어 **이후 모든 검증이 싸진다.**

**A를 마지막에 두는 이유**: 골든 갱신 판단은 이 프로젝트에서 가장 비싼 판단이고
(*"의도된 변경인가 버그인가"*는 사람만 답할 수 있다), 흩어서 3번 하는 것보다
한 번에 모아서 1번 하는 게 싸고 안전하다.
