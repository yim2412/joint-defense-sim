# 로드맵 정본 — v21 이후

> **이 파일이 살아 있는 계획의 유일한 자리다.** (2026-09-12 재정립)
>
> **작성 규칙**(전역 CLAUDE.md 5번 — Domynion 2026-09-04 실측):
> - **얹지 말고 고친다.** 세션마다 앞에 새 블록을 쌓지 않는다. 끝난 줄은 **지운다** — 근거는
>   changelog·감사보고서·SESSION_LOG 에 이미 있다.
> - 날짜별 스냅샷이 필요하면 이 파일이 아니라 `SESSION_LOG.md` 에.
> - `patch_queue` 메모리는 **이 파일의 요약 포인터**로만 유지한다(계획 본문을 양쪽에 두지 않는다).

---

## 현재 위치 (2026-09-12)

```
v11 ─ v12 ─ v15 ─ v16 ─ v17 ─ v18 ─ v19 ─ v20 ─ v21   ✅ 완주
기본   물리   AI   도메인  군수   해군   공군   육군   합동
                          캠페인
                                                  ↓
                                            v22 = 이 문서
```

`APP_VERSION v21.04.02` · v21 블록 종합 9영역 감사 통과(2026-07-24) · 워킹트리 클린.
2026-07-14 로드맵(A~E)의 **A·B·C·D 완료**, E(선택 트랙)만 남아 "정해진 다음"이 비어 있었다.

---

## 축

> ### 22번째 기능보다, 이미 쌓은 21개를 쓸 수 있게 만드는 것이 낫다.

**근거(실측 2026-09-12)**

| 잰 것 | 값 | 뜻 |
|---|---|---|
| 완주 메이저 | 21개 | 기능 축적은 충분하다 |
| GitHub Release | **0건** | 개발 PC 밖에서 **실행 불가** |
| `dist/` 크기 | **1.2 GB** (torch 365M · pyarrow 80M = **37%가 미사용 의심**) | 배포가 물리적으로 막혀 있다 |
| baseline 메모리 | **25개**, 다수 stale · **어느 것이 stale인지 기록 없음** | 수치 신뢰도의 근거가 흐리다 |
| 계획 문서 | **4곳 분산** · `patch_queue` 653줄/157KB(완료 190건) | 세션마다 "무엇부터"를 다시 판단 |
| 도메인 분석 산출물 | **0건** | 분석 도구인데 **분석 결과가 없다** |

21개 메이저를 쌓은 시점에서 22번째 기능의 한계효용은 급격히 떨어진다.
반면 **실행할 수 없고 · 수치를 얼마나 믿을지 기록이 없고 · 무슨 결론을 냈는지 말할 게 없다.**

---

## 갈래 ① 믿을 수 있게 — 싸고 확실, 먼저 친다

### ~~1-a. 감사 개선 숙제 2건~~ ✅ **완료 (2026-09-12)**
**둘 다 일부러 깨뜨려 FAIL을 확인**했다(통과는 증거가 아니다).
- **`audit_perf` 하한 가드** — 시간만 재선 잘못된 기준선을 못 잡는다는 게 89ms 사례의 핵심이라,
  **일감(work)을 함께 저장·대조**하게 바꿨다(single·battle=`sim_time` / campaign=`end_h`).
  검사 3종: ①타당성 하한(일감 단위당 시간이 하한 미만 = 측정 오류 의심 → **저장 거부**)
  ②일감 불일치(= 같은 이름으로 다른 것을 재고 있다) ③급감 0.5배(경고, FAIL 아님).
  기준선 형식 확장 + 구형식(float) 하위호환.
- **캠페인 스모크 JCS assertion** — `합동 화력 방식` 콤보를 **'동시 공격'**으로 돌려
  협조 미비 경고를 유발하고 `⚖ JCS 충돌 경고 N건` 배너를 실검증. 음성 대조(시차 공격)에서
  다른 배너는 다 뜨는데 JCS만 없어 정확히 FAIL.
  ⚠ **`cb.item_texts()`는 이 UIA 래퍼에 없다**(AttributeError) — 콤보는 `window_text()`=라벨,
  `selected_text()`=선택값으로 다룬다. 설정 후 되읽어 검증.

### ~~1-b. 번들 과잉 검사 신설 + 다이어트~~ ✅ **완료 (v21.05.01, 2026-09-12)**
**1.2GB → 734MB (445MB·39% 감소) · 빌드 384s → 170s.** torch·pyarrow·SB3·gymnasium·sympy를
spec `excludes`에 명시(원인 = `collect_submodules('sklearn')`이 array-API 경로까지 수집하며
optional 의존을 끌어옴). sklearn·joblib은 보존 — joblib은 순수 파이썬이라 PYZ 아카이브에
146개 모듈로 들어간다(폴더 부재가 정상). **검증**: 단발·캠페인 GUI 스모크 PASS(캠페인 로그
`예측모델 적용` = joblib 언피클 생존 증거) · 회귀 38×29 bit-identical · `chk_bundle_excess`
신설(일부러 깨뜨려 FAIL 확인).

### ~~1-c. baseline 재기준 앵커 1개~~ ✅ **완료 (2026-09-12)**
**25개 중 19개가 자기가 낡았는지 모르는 상태였다.** 재기준 정보가 두 메모리 본문에만 흩어져 있어
어떤 값을 믿어도 되는지 판정할 수 없었다 → **[[project-baseline-rebase-index]] 신설**(판정 정본).
- **앵커 실측**(MC 500회, 11분 29초): 요격률 **14.2%±3.6%**(v18.04.09 대비 +2.4%p) · 전멸률 100% ·
  비용 **$92.9M**(대비 **3.4배**) · 위협 **151발**.
- **🔑 비용 3.4배의 주원인은 단가가 아니라 편성이었다** — 같은 프리셋 위협이 **70→151발**로 2배 늘었다
  (v16~v21 적 편대 확충 누적). "비용 stale = 단가 정정 탓"으로만 알면 틀린다.
- **26개 메모리에 stale 배너 일괄 부착** — 측정 버전을 재기준 사건 5개와 대조해 **개별 판정**
  (`target_difficulty`=1개 사건, `cost_rebase`=3개, 나머지=4개). 방향·메커니즘 결론은 유효,
  절대 수치만 못 쓴다는 점과 ON/OFF 비교는 그대로 유효하다는 점을 배너에 명시.
- **프로브 규칙의 빈틈 발견**: 재발 방지 규칙 *"`total_threats`를 항상 출력"*은 **MC 경로에서
  지킬 수 없다**(`monte_carlo_v7` 반환에 그 키가 없다) → MC 프로브는 단발 1회로 별도 확인.

> **"게임이 아닌 분석 도구" 포지셔닝에서 수치 신뢰도가 곧 제품이다.**

---

## 갈래 ② 실행 가능하게

### 2. 릴리스 exe 배포 — ⏸ **준비 완료 · 공개 보류** (2026-09-12 사용자 결정)

**draft까지 만들어 두고 공개는 하지 않았다.** 사용자가 *"아직은 공개하지 말자"* 로 판단.

- **준비된 것**: `joint-defense-sim-v21.05.01-win64.zip` **302 MB**(727MB→302MB, 24초 압축) ·
  draft 릴리스 생성 완료(자산 업로드 끝) · 릴리스 노트 작성(실행 3단계 · **SmartScreen 경고 안내** ·
  요구사항 · 기능 7줄 · 영어 요약 · PolyForm-NC).
- **한글 파일명 검증 완료**: zip 3311개 항목 중 한글 86개 **전부 UTF-8 플래그**(미설정 0).
  최상위 폴더는 ASCII(`JointDefenseSim`)로 바꿔 압축 — `Compress-Archive`는 인코딩 지정이 안 되므로
  .NET `ZipFile.CreateFromDirectory`에 `Encoding.UTF8`을 명시해야 한다. 7-Zip은 이 PC에 없다.
- **지금 상태**: draft는 **저장소 쓰기 권한자만** 볼 수 있어 노출 0. **태그 `v21.05.01`도 아직 미생성.**
- **공개하려면**: `gh release edit v21.05.01 -R yim2412/joint-defense-sim --draft=false`
  → 그 다음 README·`docs/index.html`에 다운로드 링크 추가·커밋. **링크는 아직 넣지 않았다**
  (릴리스가 없으면 깨진 링크가 된다).
- **없애려면**: `gh release delete v21.05.01 -R yim2412/joint-defense-sim --yes` (흔적 없이 사라진다).

## 갈래 ③ 말할 수 있게 — 어느 계획에도 없던 것

### ~~3. 대표 분석 사례 1건~~ ✅ **완료 (2026-09-12)** — 정본 `docs/analysis/01_편대_생존_분석.md`

"공개용 산출물"이 아니라 **도구 자체에 대한 첫 실전 검증**으로 재정의해 수행했다.
21개 메이저를 쌓는 동안 검증은 전부 "기능이 도는가"였고, **이 도구로 분석을 끝까지 해본 적이
한 번도 없었다.** 실제 분석 질문 하나를 관통시키자 **핵심 기능이 반대 결론을 내는 것**이 나왔다.

- **실험 1**(편성 1→9척, MC 60회): 어떤 편성으로도 랴오닝 항모전단 상대 전멸을 못 면한다
  (9척 1함대 93%가 최선). 그런데 **요격률이 편성 규모에 반비례**(19.1%→8.4%)한다.
- **실험 2**: 원인 규명 — **같은 적 프리셋인데 아군 편성에 따라 적이 139~438발(3.2배)을 쏜다.**
  큰 편성일수록 오래 버티고(1925→3422초) 그동안 위협이 누적된다 → **요격률의 분모가 통제되지
  않는다** → 편성 간 요격률 비교는 **같은 시험이 아니다**.
- **실험 3**: 그 실질 영향 — `recommend_fleet_v7`(요격률 0.6 + 생존율 0.4)을 실제로 돌리면
  **추천이 역순**이다. 구축함 1척이 2위, **이지스 8척 편성이 꼴찌**. 전멸 100%라 생존율 항이
  전부 0으로 붕괴해 성능점수가 사실상 요격률 단독이 된다.
  ⚠ docstring은 *"동일 위협에 대해 MC 평가"* 라고 적고 있는데 **그 전제가 성립하지 않는다**.

## 그 다음 — 기능 트랙 (지금은 안 건드림, 순서만 고정)

1. ✅ ~~**항모 함재기 무한 생산 정정**~~ — **v21.06.01 완료**(B안: `enable_wing_cap` 실험적·기본 OFF). 전멸률 97.5→30.0%. **남은 것 = 기본값 ON 승격 판단**(골든 전면 갱신 수반)
2. ✅ ~~**편성 비교 지표 정정**~~ — **v21.06.04 완료**.
   성능식 교체(`생존율 0.5 + 교환비 0.3 + 요격률 0.2`). **역순이 뒤집혔다** —
   구축함 1척 2위→**꼴찌**, 한미 기동전단 강화 꼴찌→**1위**.
   가중치 안정성 8조합: 완전 동일 3/8 · 1위 유지 6/8 · **꼴찌 8/8 동일**(핵심 교정은 무관).
   교환비 0.3은 "생존만 하고 적을 못 죽이는 편성"을 거르는 최소선이라 임의 수치가 아니다.
   정본 `docs/analysis/02_편성비교_지표.md`.

3. **전력 DB 심화 묶음** — `_PLANS` 선택① + 정밀화 **B2**(적 대공위협) + **C1**(Mk.46 실제 제원)
   + **C5**(극초음속 단계별 속도) + 감사 ②. **세 계획이 한 점에서 만난다.**
   `_BASE_AIR_THREAT = 6.0` 상수 하나가 적 공군 전체를 추상하는 건 명백한 결함.
4. **E1 캠페인 MC 병렬화** — 300~1000회 → 10,000회+. 분포 꼬리(드문 패배) 정확도.
5. **v16.1 대잠전 균형** — `_PLANS` **유일 미완 버전 항목**. 잠수함 교전 방식(접근·재공격·매복)
   **구조 변경**. ⚠ 수치 튜닝 재시도 금지([[project-asw-emcon-finding]]). **두 번 막다른 길을
   겪은 영역**이라 가치 대비 위험이 높다 — **미완으로 남겨두는 것도 결정이다.**
6. **자율 학습 AI 심화** — 인프라(self-play·자가개선 루프) 완성. 남은 건 *더 깊은 전장·넓은
   행동공간* 이 선행돼야 해서, 지금 밀면 이미 확인된 천장에 부딪힌다.
7. UI 영어화 · 정밀화 '하' 12건(A3·A5·B3·C2·C3·C4·D2~D5·E2).

---

## 미채택 / 트리거 대기 — 다시 논의하지 않기 위해 명시

| 항목 | 상태 |
|---|---|
| 멀티플레이어 대전 | **미채택** (2026-07-24 사용자 결정) |
| 단발 교전 모델 삭제 | **트리거 미충족** — surrogate · numpy 고속화 · 고속성 포기 셋 중 하나 필요. **존속 확정** |
| 레이저(v17.2) | **음성 확정 종결** — 재시도 금지 [[project-laser-efficacy-finding]] |
| 대잠 EMCON 수치 튜닝 | **막다른 길** — 구조 변경만 유효 [[project-asw-emcon-finding]] |
| 정밀화 B1(격자 촘촘화) · D1(haversine) | **제외** — 개선이 역효과 |

---

## 계획 문서 지도 — 무엇이 어디에 있나

| 자리 | 역할 |
|---|---|
| **`plan_roadmap.md`** (이 파일) | **살아 있는 계획 정본.** 여기만 고친다 |
| `app_launcher.py` `_PLANS` | **사용자에게 보이는** 공식 로드맵(exe 향후 계획 탭). 용어 규칙 적용 |
| `SESSION_LOG.md` | 세션 매듭 판단 맥락(왜/다음/미커밋). 날짜별 누적 |
| `patch_queue` 메모리 | 이 파일의 **요약 포인터**. 계획 본문 중복 금지 |
| `_archive/plans/` | 구현 완료된 설계 문서 |
| `감사보고서.md` | 블록별 감사 결과 누적 |

**정리 대상**: `patch_queue.md` 653줄/157KB 중 완료 190건 → `_archive/` 이관
(전역 5번 — 끝난 기록을 **지우지는 않는다**. 대부분이 *왜 그 결론에 이르렀는가* 다).
`plan_precision_upgrades.md` 의 살아 있는 항목은 위 '기능 트랙' 에 흡수됨 — 원본은 상세 참조용 유지.

---

## 착수 순서

```
0. 이 파일 작성 + patch_queue 포인터화        ← 지금
1. ~~1-b 번들 다이어트~~ ✅ 완료 (1.2GB→734MB · 빌드 384s→170s)
2. ~~1-a 감사 숙제 2건~~ ✅ 완료
3. ~~1-c baseline 앵커~~ ✅ 완료
4. ⏸ 2  릴리스 exe — draft 준비 완료 · **공개 보류**(사용자 결정)
5. ~~3 대표 분석 사례~~ ✅ 완료 → **편성 비교 지표 결함 발견(v22 1순위 후보)**
```

**1-b 를 먼저 두는 이유**: 오늘 나온 발견이고, 남은 불확실성이 **사고가 아니라 측정으로 풀리는
종류**다. 빌드 한 번이 계획의 정확도를 더 올린다. 고치면 ②가 바로 현실이 된다.
