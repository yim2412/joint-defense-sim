# 로드맵 정본 — v22

> **이 파일이 살아 있는 계획의 유일한 자리다.** (2026-09-19 축 재정립)
>
> **작성 규칙**(전역 CLAUDE.md 5번):
> - **얹지 말고 고친다.** 세션마다 앞에 새 블록을 쌓지 않는다. 끝난 줄은 **지운다** —
>   근거는 changelog·감사보고서·SESSION_LOG 에 이미 있다.
> - 날짜별 스냅샷이 필요하면 이 파일이 아니라 `SESSION_LOG.md` 에.
> - `patch_queue` 메모리는 **이 파일의 요약 포인터**로만 유지한다.

---

## 현재 위치 (2026-09-20 — 전면 분석 후 재정립)

```
v11 ─ v12 ─ v15 ─ v16 ─ v17 ─ v18 ─ v19 ─ v20 ─ v21   ✅ 완주
기본   물리   AI   도메인  군수   해군   공군   육군   합동
                          캠페인
                                                  ↓
                                            v22 = 이 문서
```

`APP_VERSION v21.11.03` · 정적 스캔 **62/62 PASS** · 회귀 **54케이스 × 32지표 PASS**
· pre-push 런타임 감사 5종 PASS. v21 블록 종합 9영역 감사 통과(2026-07-24).
**전면 분석 트랙 완료**(2026-09-20, 발견 15건 중 14건 종결 — `analysis/00_SUMMARY.md`).

### 이번 재정립에서 바뀐 것 (요약)

| | 이전 계획의 전제 | 재측정 결과 |
|---|---|---|
| 편성 비교 | *"편성을 키우면 요격률이 절반 아래로 떨어진다"*(역전) | **역전 없음.** 이미 묶음 A·B에서 해소됐는데 문서가 stale 이었다 |
| v22 1순위 | "분모 미통제" 한 덩어리 | **세 층으로 분해** — (a) 정의 불일치 ✅닫힘 (b) 노출 시간 ❌열림 (c) 함재기 생산 ⚠부분 |
| 안전망 | 정적 54 · 골든 42 | **정적 62 · 골든 54** — 무거운 변경을 더 싸게 검증할 수 있다 |

---

## 축 — 트레이드 스터디 도구 (2026-09-19 확정 · 2026-09-20 근거 갱신)

> ### **"이 위협에 대해, 어떤 편성이 요구조건을 충족하는가 — 얼마에."**

축 자체는 **유지**한다. 다만 *"요격률을 강등한다"* 의 근거가 **역전 → 변별력 부족**으로
바뀌었다(아래 근거 1). 결론은 같고 이유가 달라졌으므로 처방의 강도도 달라진다 —
**요격률을 없앨 일이 아니라, 분모를 명시하고 보조 지표로 내리는 일이다.**

### 판정 규칙 — 모든 기능·화면·수정에 이것 하나를 묻는다

> **"이게 『편성 A와 B 중 무엇을 고를 것인가』에 답하는 데 기여하는가?"**

| 항목 | 판정 | 처리 |
|---|:--:|---|
| 교환비 · 방어 포화도 · 요격당 비용 · 탄약 효율 | ✅ | **1면으로 승격** |
| 요격률 (편성 간 **변별력 부족** — 분모가 편성마다 다름) | ⚠ | 1면에서 **강등**하되 **분모를 함께 표기**(= "미사일 N발 중") |
| MC 기본 1.1시간 · 정밀 모드 14시간 | ❌ | **반복 질의 불가 = 치명적.** 축소·제거 |
| BMD 5계층 · 합동 화력 (기본 OFF) | ✅ 기여해야 함 | **기본 ON 승격 판단** |
| 3D 전장 | ❌ | 유지하되 **별도 트랙** |
| AI/RL · self-play | ❌ | **동결** |
| 감사 인프라 (정적·골든·property·effect) | ✅ 토대 | 유지·강화 — **이번에 62항목·54케이스로 키웠다** |

**이 선택의 비용(명시):** ① 3D 개선이 후순위로 내려간다 ② 공군·육군·합동 층은
캠페인 모드 전용이라 **단발 편성 트레이드 스터디에 직접 기여하지 않는다**. 삭제가 아니라
당분간 우선순위를 못 받는다는 뜻이다. 이 비용을 알고 택했다.

---

## 근거 — 2026-09-20 재측정 (이전 근거를 대체한다)

> **왜 다시 쟀나**: 전면 분석이 요격률 분모 정의를 고쳤다(F-005·F-008). 계획의 헤드라인
> 근거가 그 지표였으므로, **고친 뒤에도 같은 말을 할 수 있는지**부터 확인했다.
> 방법: 분석 시작 전 커밋(`340d302`)을 `git worktree` 로 뽑아 **같은 조건**으로 대조.

### 1. 역전은 없다 — 다만 **변별력이 없다**

적=랴오닝 항모전단 고정 · 맑음(주간) · 단발 5시드 평균

| 아군 편성 | 함정 | 분석 전 위협 | 분석 전 요격률 | **분석 후 위협** | **분석 후 요격률** |
|---|---:|---:|---:|---:|---:|
| 단독 작전 | 1 | 70 | 40.0% | **62** | **45.8%** |
| 이지스 기동전단 | 6 | 130 | 44.1% | **122** | **47.0%** |
| 전 이지스 기동전단 | 7 | 123 | 75.1% | **115** | **80.3%** |

**두 가지가 드러났다.**

- **이전 계획의 표(21.7% → 16.1% → 12.5%, 역전)는 stale 이었다.** 분석 시작 *전* 에
  이미 역전이 없다(40.0 → 44.1 → 75.1). 묶음 A·B(v21.08, 프리셋 현실화·`wing_cap`)가
  해소했는데 **문서가 따라가지 않았다.** → 그래서 "v22 1순위 = 분모 미통제"가 실제보다
  **과대평가**돼 있었다.
- **내 수정의 효과는 전 편성 정확히 −8**(= 파도로 발진한 함재기 플랫폼이 분모에서 빠짐).
  방향을 바꾼 게 아니라 **크기를 다듬었다.**

**그래도 문제는 남는다**: 함정 1척(45.8%)과 6척(47.0%)의 차이가 **1.2%p** 다.
7척에서만 80.3%로 뛴다. 역전은 아니지만 **편성 선택을 이 숫자로 할 수 없다.**
분모가 편성마다 다르기 때문이다(62 vs 122 vs 115) — 오래 버티는 편성이 더 많이 맞는다.

### 2. 분모 문제는 세 층이었다 (분해해야 계획이 정확해진다)

| 층 | 내용 | 상태 |
|---|---|:--:|
| (a) **정의 불일치** | 초기 편성은 미사일만, 파도 스폰은 플랫폼까지 셌다 | ✅ **닫힘**(F-005·F-008, v21.11.02) |
| (b) **노출 시간 비통제** | 오래 버티는 편성일수록 적이 더 쏜다 → 분모가 커진다 | ❌ **열림 — 이게 진짜 v22 1순위** |
| (c) **함재기 무한 생산** | 단발 모드는 `_enforce_wing_cap` 이 꺼져 있어 계속 발진 | ⚠ **부분** — 분모에선 빠졌으나 교전 자체엔 계속 참여 |

(b)의 처방은 **지표를 바꾸는 것**이지 엔진을 바꾸는 게 아니다 —
*"고정 위협 N발에 대해 몇 발을 막았나"* 로 **분모를 통제**하거나,
**교환비·요격당 비용**처럼 애초에 분모가 편성에 안 걸리는 지표를 1면에 둔다.

### 3. 안전망이 커져서 무거운 변경이 싸졌다

| | 이전 | 지금 |
|---|---:|---:|
| 정적 스캔 | 54항목 | **62항목** |
| 회귀 골든 | 42케이스 | **54케이스** |
| pre-commit | 41초 | 약 45초 |
| pre-push | 181초 | 약 190초 |

**이번에 처음 감시 아래 들어온 것**: 파도 스폰 경로 · 기본 OFF 토글 9종(정찰 드론·자율
교전·기뢰전·레이저·RAS 등) · Pk 불확실성. **검증은 했는데 지키는 장치가 없던** 기능들이다.
→ (b) 같은 **지표 정의 변경**을 이제 더 안전하게 할 수 있다.

### 4-b. 또 하나의 stale — 등급 배너는 이미 고쳐져 있었다

이전 근거 2(*"등급 배너가 실측과 반대 처방을 낸다 — '함정 수를 보강하세요'"*)도 **stale**이다.
**묶음 B 에서 이미 포화도 기반으로 바뀌어** 있었다(`mixin_resultpanel.py`):

```
sat >= 1.0  → "동시 위협이 교전 채널을 초과합니다(포화도 N배) — CEC 로 채널을 분산하거나
               편대를 나눠 배치하세요. 이 구간에서는 함정만 늘려도 요격률이 오르지 않습니다."
sat >= 0.80 → "채널 사용률 N% — 위협이 조금만 늘어도 포화합니다."
그 외        → "장거리 SAM 재고·요격 무기 구성을 조정하면 향상 여지가 있습니다."
```

**D1 을 착수하면서 코드를 열어 보고서야 알았다.** 근거 1과 같은 부류 —
*고쳐 놓고 문서를 안 고쳤다.* 이번 재정립에서 **두 번째로 나온 같은 실수**다.
→ 아래 '계획을 다시 흔들 조건' 2번(6개월 미재측정 근거는 불신)을 **"문서가 아니라
코드를 열어 확인한다"** 로 강화한다.

### 4-c. 아직 유효한 이전 근거 (다시 재지 않았다)

- **기본 실행 1.1시간** — 반복 질의가 본질인 트레이드 스터디에서 치명적. 미해결.
- **위협 추적 컬럼 공백**(유형 `?` · 탐지거리 0 · 격추거리 75% 공백) — 미해결.
- **기본 설정이 v7 시절 모델**(체크박스 54개 중 24개가 "(실험적)") — 미해결.
  이번에 **하나 더 늘었다**(`enable_pk_uncertainty`, 실험적·기본 OFF).
- **작은 글씨에 판단 근거**(10px가 계산 근거) — 미해결.

> 시나리오 난이도 역전(이전 근거 6)은 **묶음 A 에서 해소**됐다.

---

## 묶음 C — 회귀 무관 ✅ 완료 (2026-09-19, v21.07.01~17)

표시 오류 5건 · 엔진 값 화면 연결 3건 · 정밀도 횟수 현실화 · 가독성 5건 ·
위생 5건. 상세는 changelog와 커밋 메시지에.

**여기서 나온 것 중 계획에 영향을 준 것만 남긴다:**
- **기본 실행 1.1시간 → 약 9분**(빠름 2.3분 / 정밀 106분). 이후 모든 검증이 싸졌다.
  근거: 요격률 시행 간 표준편차 **4.67%p 실측** → 1,000회면 평균 오차 ±0.148%p.
- **`REPORT_TITLE`·`AIRCRAFT_SPECS`·`SIM_MODE_PRESETS` 3개 상수를 신설**해 흩어져
  있던 값을 한 곳으로 모았다. 특히 정밀도 횟수는 **4곳에 흩어져 있었고 한 곳은
  값을 키로 역참조**해, 값을 바꾸면 조용히 기본값으로 떨어지는 구조였다.
- **탐지거리는 '스폰 거리'가 아니라 '최초 포착 거리'로 정의**했다(45~458km 실측).
  어뢰는 대공 레이더를 안 지나므로 기록되지 않는다 — 표에 '—'로 나온다.
- 정적 스캐너가 `anti_sam` 제거를 즉시 '숨은 플래그'로 잡았다. 안전망이 의도대로
  작동한 사례라, `EFFECT_DEAD`를 그 검사에서 면제하는 것으로 정정했다.

**남긴 것(의도적)**: 열 재배치(해역·항공 자산 열의 공백 vs 환경·방어전술 열의 과밀)는
손대지 않았다 — 레이아웃 구조 변경이라 회귀 위험 0이라는 묶음 C의 전제에서 벗어난다.
실제 창에서 보고 판단할 일이라 **별도 트랙 6(UI 감사 도구)과 함께** 다룬다.

## 묶음 A·B — ✅ 완료 (2026-09-19, v21.08.01~08)

**A(무대 만들기)와 B(지표 바로잡기)를 같은 날 끝냈다.** 상세는 `plan_a_defaults.md`와
`docs/analysis/03`에, 결과는 changelog에.

**핵심만 남긴다:**
- **랴오닝 대역이 방어 불가였던 원인 = 항모 함재기 80발**(탑재 104발인데 위협 190발).
  `_CARRIER_STRIKE_FRACTION=0.35`로 대함 투입분만 상한(보유 대수는 공개 제원이라 유지).
  → 7척 요격률 **11.4%→84.8%** · 전 편성 전멸 해소. **골든 갱신 1회**(4/8 케이스).
- **'입체 포화(최강)' 37→107발** 현실화. 7척 요격률 100%→88.1%.
  ⚠ 난이도 서열은 편성에 따라 여전히 엇갈린다 — 엄밀히 세우려면 **프리셋 전수를 같은
  축으로 재는 작업**이 별도로 필요하다(미착수).
- **방어 포화도를 1면에 병기**(요격률 교체가 아니다 — 4개 대역에서 요격률은 멀쩡했다).
  MC 3경로+LHS 4곳에 집계 추가. 등급 배너 처방이 포화도에 따라 갈린다.
- **REQ 임계값을 분석자 입력으로**. 고정값은 모델이 바뀔 때마다 변별력을 잃는다
  (실측: REQ-04·05 전부 실패 / REQ-08 전부 통과 / REQ-02·07은 되살아남).
- **기본 설정 현대화**: PNG 기본 ON · 초기값↔복원 불일치 5건 해소 ·
  '(실험적)' 26→18개 · BMD는 기본 OFF 유지(환경물리가 아니라 전력 배치라서).

**남은 것**: '(실험적)' 18개 중 캠페인·전장 층 11개는 그 모드가 정규화될 때 함께 판단.
REQ-04·05·08의 기본 임계값 자체를 다시 고르는 일은 하지 않았다 — 입력으로 뺐으므로
**분석자가 시나리오마다 정하는 것이 맞다**고 봤다.

## 별도 트랙 — 살아 있는 것만 (완료분은 지웠다 · 전역 규칙 5)

> 끝난 항목(3D 표현 · UI 감사 도구 · 프리셋 난이도 전수 · 위협 집계 정의 · 캠페인
> 효과 게이트)은 **여기서 지운다.** 근거는 changelog·감사보고서·`SESSION_LOG` 에 남아 있다.
> 살아 있는 것은 **아래 '후순위' 표(E1~E8)로 통합**했다 — 두 곳에 흩어져 있으면
> 어느 것이 최신인지 날짜로만 구분하게 된다(전역 규칙 5의 Domynion 실측 교훈).

**남은 항목의 상세**는 후순위 표에 한 줄씩 있고, 긴 설명이 필요한 둘만 여기 둔다:

- **E3 프리셋 위협 규모 현실화 + 자폭 발현 무대** — 29종 중 **18종이 생존 100%**
  (이지스 6척 기준)라 실질 변별 구간이 11종뿐이다. 위협이 한 자릿수라 교전이 성립조차
  않는 것들: `북한 입체 공격`(4발)·`무인기 군집 포화`(4발)·`대잠 복합`(6발)·
  `북한 탄도 포화`(6발)·`러시아 극초음속`(6발). 묶음 A 가 '입체 포화'에 한 일을
  나머지에도 하는 것인데 **골든 전면 갱신을 부른다.**
  + 자폭 메커니즘에 **발현 무대가 없다**(기함 200m 도달 전 전부 격퇴, `docs/analysis/04`).
- **E6 v16.1 대잠전 균형** — `_PLANS` 유일 미완 버전 항목.
  ⚠ **수치 튜닝 재시도 금지**([[project-asw-emcon-finding]] — 구조 변경만 유효).
  **미완으로 남겨두는 것도 결정이다.**


## 미채택 / 트리거 대기 — 다시 논의하지 않기 위해 명시

| 항목 | 상태 |
|---|---|
| **AI/RL · self-play 트랙** | **동결**(2026-09-19). 기본 OFF·실험적·"이미 확인된 천장". 축 밖 |
| 릴리스 exe 공개 | **draft 준비 완료 · 공개 보류**(2026-09-12 사용자 결정). 태그 미생성 |
| 멀티플레이어 대전 | **미채택**(2026-07-24) |
| 단발 교전 모델 삭제 | **트리거 미충족 · 존속 확정** |
| 레이저(v17.2) | **음성 확정 종결** [[project-laser-efficacy-finding]] |
| 대잠 EMCON 수치 튜닝 | **막다른 길** — 구조 변경만 유효 [[project-asw-emcon-finding]] |
| 정밀화 B1(격자 촘촘화) · D1(haversine) | **제외** — 개선이 역효과 |
| "분석/시연 2모드 분리" | **미채택**(2026-09-19). 기본 설정 2개 = 검증 표면 2배. 전역 규칙 9(YAGNI) |

---

## 계획 문서 지도

| 자리 | 역할 |
|---|---|
| **`plan_roadmap.md`** (이 파일) | **살아 있는 계획 정본.** 여기만 고친다 |
| `app_launcher.py` `_PLANS` | **사용자에게 보이는** 로드맵(exe 향후 계획 탭). 용어 규칙 적용 |
| `SESSION_LOG.md` | 세션 매듭 판단 맥락(왜/다음/미커밋). 날짜별 누적 |
| `patch_queue` 메모리 | 이 파일의 **요약 포인터**. 계획 본문 중복 금지 |
| `docs/analysis/` | 도구 자체 검증 기록(01 편대 생존 · 02 편성비교 지표) |
| `_archive/plans/` | 구현 완료된 설계 문서 |
| `감사보고서.md` | 블록별 감사 결과 누적 |

---

## 착수 순서 (2026-09-20 재정립)

```
1. 묶음 C  ✅ 완료 (v21.07.01~17)
2. 묶음 A  ✅ 완료 (v21.08.01~05, 골든 1회 갱신)
3. 묶음 B  ✅ 완료 (v21.08.06~08)
4. 별도 트랙 — 3D ✅ · UI 감사 도구 ✅ · 프리셋 난이도 전수 ✅ · 위협 집계 정의 ✅
5. 전면 분석 트랙 ✅ 완료 (2026-09-20) — 규약 수립 → 0~5단계 분석 → B단계 수정 6묶음
6. 분석 결과 수정 ✅ 완료 (v21.11.01~03) — 발견 15건 중 14건 종결
7. D1 ✅(v21.12.01) · D2 ✅(v21.12.02) · D4 ✅(v21.12.03) → ← **지금 여기: D5 → D3**
```

### 재정립의 근거 한 줄

> 이전 1순위였던 *"요격률 분모 미통제"* 는 **세 층이었고 그중 하나만 진짜 남았다**(근거 2).
> 그래서 1순위를 **"분모를 통제한 지표를 1면에 세운다"** 로 다시 쓴다 —
> 엔진을 고치는 일이 아니라 **무엇을 보여줄지 고르는 일**이다.

---

### D1. 편성 비교가 성립하는 1면 — ✅ **완료** (2026-09-20, v21.12.01)

> 1면 카드 9→11개. **교환비·요격당 비용·방어 포화도를 앞 세 자리로**, 요격률은 4번째로
> 내리고 **분모를 병기**(`47.0% (57/122발)`). 값은 이미 전황 지표판이 계산하고 있었고
> 1면에만 없었다 — 표시 계층만 바꿔 골든 무영향. 판정 `p016` FAIL→PASS.
> **남은 것**: REQ-01(요격률 95% 문턱) 재검토 — 분모가 바뀌었으니 문턱도 다시 봐야 한다.
> 게이지는 여전히 요격률 기반이다(교체는 별도 판단).

**왜 1순위인가**: 축(*"편성 A와 B 중 무엇을 고를 것인가"*)에 직접 답하는 유일한 항목이고,
지금 1면 숫자(요격률)는 **1척과 6척을 1.2%p 로밖에 구분하지 못한다**(근거 1).
분모 정의는 이미 고쳤으므로(v21.11.02) **남은 것은 표시 계층**이다.

| 할 일 | 비고 |
|---|---|
| **교환비 · 요격당 비용 · 방어 포화도**를 1면 주지표로 | 분모가 편성에 안 걸리는 지표들 |
| 요격률은 **분모를 함께 표기**해 보조로 강등 | *"미사일 122발 중 57발"* 식 |
| **등급 배너 처방 문구 정정** | 지금도 *"함정 수를 보강하면 향상"* 이라고 쓴다 — 1.2%p 차이를 두고 하는 말이라 근거가 없다 |
| REQ-01(요격률 기준) 재검토 | 분모가 바뀌었으니 문턱도 다시 봐야 한다 |

**반경**: U(UI·렌더) 중심 + REQ 판정이 걸리면 E. **판정**: S(1면 지표 구성 정적 검사) + 스모크.
**선행 조건 없음** — 지금 바로 가능.

### D2. 반복 질의가 가능한 실행 시간 — ✅ **완료** (2026-09-20, v21.12.02)

> **절반은 이미 돼 있었다**(묶음 C-4 가 MC 10,000→1,000, 표준 약 9분). 남은 절반인
> **조기 수렴 종료**를 구현했다 — 표준 모드에서 200회 요청이 **50회·53초**로 끝난다
> (기본 OFF 251초 대비 **75% 절감**, 평균 차이 0.0027 = 문턱 ±1%p 안).
> 모드별 문턱: 빠름 0.015 · 표준 0.010 · 정밀 0.003(사실상 전 회차 수행).
> 안전장치: 최소 회차 50 · 10회마다 검사 · 엔진 기본값 0=끄기(하위 호환).

**왜 2순위**: 트레이드 스터디는 **같은 질문을 편성만 바꿔 여러 번** 던지는 일이다.
기본 1.1시간이면 D1 을 아무리 잘 만들어도 **쓰이지 않는다.**

- 기본 MC 10,000 → **500~1,000**(요격률 표준편차 ±3.6%p 기준 충분, 이전 근거 3)
- 조기 수렴 종료 실제 구현(`ConvergenceWidget` 은 지금 표시 전용)
- 정밀 모드는 남기되 **기본에서 분리**

**반경**: E·U. **판정**: P(실행 시간 측정 프로브 — 목표 문턱 명시).

> **순서 변경(2026-09-20)**: D3 → D4 → D5 가 아니라 **D4 → D5 → D3** 이다.
> D3 의 승격 판정은 *기준값 측정*(D5)이 있어야 가능한데, D5 는 측정만 하는 판이라
> 먼저 해도 막히지 않는다. D4(PNG 승격 한 줄)는 독립이라 가장 싸다.

### D3. 기본 설정을 현재 모델로 (실험적 **18개** 정리 — 실행 기반 카운트)

**왜 3순위**: D1·D2 가 끝나면 *"그래서 기본값으로 무엇을 보여줄 것인가"* 가 남는다.
체크박스 54개 중 **24개(44%)가 "(실험적)"** 이고 **BMD 5계층이 전부 기본 OFF** 다 —
README 가 헤드라인으로 광고하는 기능이 꺼져 있다.

- `CLAUDE.md` **실험적→정규 승격 게이트**대로 하나씩 판정(기준값 측정 → 레이블 제거 → 기본값 결정)
- **`enable_pk_uncertainty`**(v21.11.03 신설)도 이 대열에 있다 — 기준값 측정 후 승격 판단
- 신규 실행 / 저장본 복원의 **기본값 불일치**(ON 15/36 vs 8/40) 해소

**선행 조건**: 승격은 **기준값 측정**이 있어야 한다 → D5 와 맞물린다.

### D4. 위협 추적 컬럼이 빈칸인 문제 — ✅ **완료** (2026-09-24, v21.12.03)

> **근거 셋 중 둘이 또 stale 이었다.** 유형 `?`·탐지거리 0 은 v21.07.06~07 에서 이미
> 고쳐졌고, *"PNG 한 줄 승격"* 도 v21.08.03 에서 이미 됐다. 그런데 **격추거리 공백은
> 실재했다**(요격 56건 중 48건) — 원인은 PNG 가 아니라 **기록 기준**이었다.
> 요격탄↔표적 거리를 근접신관 판정(≤200m) 순간에 적어 늘 0.0~0.2km → 표에서 `—`.
> 방어 함정↔격추 지점 거리로 바꿔 공백 0건(중앙값 45.7km, 탄도 포화 91.8km).
> 0.0km(직상공 CIWS)를 `—` 로 지우던 표시 조건, 기만기·회피 어뢰의 무기 공백도 함께.
> 판정 `p018` FAIL→PASS, 되돌리는 변이 2/2 빨개짐. 표시 전용이라 골든 54×32 무영향.
> **재지 않은 것**: 기만기·회피 라벨은 세 프리셋 모두 어뢰가 없어 실행으로 안 쟀다.

### D5. 미측정으로 남은 것 — 측정 자체가 작업

전면 분석이 **정직하게 `미측정` 으로 남긴 것들**이다(`analysis/00_SUMMARY.md` 5절).
D3 의 승격 판단이 여기 의존한다.

| 항목 | 왜 필요한가 |
|---|---|
| **2.2C 왜곡의 크기**(근사별 ON/OFF MC) | 실험적→정규 승격의 합격 기준이 *"ON/OFF 기준값 측정"* 이다 |
| 물리 수식 전수 표화(가정·깨지는 조건) | 2-A 가 결함 찾기에 시간을 써서 안 했다 |
| `db_*` 미배선 심볼 13개 | 측정 후 *쓸 것/지울 것* 판정(L-2) — **측정 전에 지우면 대조군이 사라진다** |

**측정 완료 (2026-09-24)**
- **Pk 불확실성 기준값** — `p019`, 이지스 기동전단 vs 랴오닝 항모전단·흐림, seed 1~300 × OFF/ON.
  요격률 표준편차 **2.86 → 3.28%p**, 더해지는 성분 ≈ **1.6%p**(분산 증가 부트스트랩 95%
  0.000031~0.000502, 0 제외), 평균 이동 −0.07%p. **60회로는 구분 안 됐다**(구간이 0 포함) —
  이 크기는 300회급에서야 보인다. D3 승격 판단의 근거는 이걸로 충분하다: 평균을 안 바꾸고
  산포만 약 15% 넓힌다.
- **MC 장시간 RSS** — `p020`. MC 워커 경로(`mc_mode=True`)는 120회 138→143MB로 **평탄**.
  매회 `gc.collect()` 하면 137MB 고정이지만 시간 +2.7% — 넣을 이유 없음.
  ⚠ 비-MC 모드(단발·프로브)는 순환 참조 쓰레기로 30회 313→385MB 까지 오른다
  (`gc.collect()` 하면 141MB). 앱의 단발 실행은 1회라 무해하지만, **비-MC 모드로
  수백 회 도는 프로브를 병렬로 돌리면 워커당 수백 MB**(p019 가 8워커 5.4GB 를 찍었다).

---

## 후순위 — 축 밖이거나 트리거 대기

| # | 항목 | 상태 |
|---|---|---|
| E1 | **F-014** v12.7 단계2(프록시가 런타임 22%) | 분석이 수치화했다. **정확도 문제가 아니라** 후순위 |
| E2 | **함재기 생산 자체**(단발 `_enforce_wing_cap` OFF) | 분모에선 빠졌지만 교전엔 계속 참여. D1 이 끝나면 영향이 보인다 |
| E3 | **프리셋 위협 규모 현실화 + 자폭 발현 무대** | 29종 중 18종이 생존 100%. **골든 전면 갱신을 부른다** |
| E4 | 전력 DB 심화(`_BASE_AIR_THREAT = 6.0`) | 층별 충실도 불균질 |
| E5 | 캠페인 MC 병렬화 | 분포 꼬리 정확도 |
| E6 | v16.1 대잠전 균형 | ⚠ 수치 튜닝 재시도 금지. **미완으로 남기는 것도 결정** |
| E7 | **규약 빈틈 3건** | 9.1 상한을 *되돌림 단위* 로 재정의 · 9.2 에 메타 항목 형식 · 생성 데이터 제외(이미 임시 반영) |
| E8 | 작은 글씨(10px에 판단 근거) | UI 위생 |

---

## 이 계획을 다시 흔들 조건 (미리 적어 둔다)

이전 계획이 stale 해진 이유는 **근거를 재지 않고 문서만 남겨서**다. 같은 일을 막으려고
**무엇이 관측되면 계획을 다시 짜는지** 미리 적는다.

1. **D1 을 끝냈는데 편성 A·B 선택이 여전히 애매하면** — 지표가 아니라 **모델**이 문제다.
   그때 E2(함재기 생산)·E3(프리셋 규모)로 축을 옮긴다.
2. **근거 표의 수치가 6개월 이상 재측정되지 않았으면** — 그 표는 신뢰하지 않는다.
   이번처럼 `git worktree` 로 과거 커밋과 대조해 다시 잰다.
   ⚠ **수치뿐 아니라 '이미 고쳐졌는지'도 코드를 열어 확인한다.** 2026-09-20 재정립에서
   근거 1(편성 역전)과 근거 2(등급 배너 처방)가 **둘 다 이미 해소돼 있었다** —
   문서만 보고 계획을 세우면 없는 문제를 1순위로 올린다.
3. **정적 스캔·골든이 한 번에 10항목 이상 늘면** — 검증 비용이 임계를 넘었는지 본다
   (현재 정적 62 · 골든 54 · pre-commit 45초 · pre-push 190초).
