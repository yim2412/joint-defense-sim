# 종합 감사 보고서

> 메이저 블록 완료 시 수행하는 **종합 감사**(9영역)의 기록.
> 규칙 정본은 `CLAUDE.md`의 '종합 감사' 섹션. 이 파일은 **산출물**이다.
>
> **사용법**: 블록 감사를 마치면 아래 형식으로 **최신이 위로 오도록** 섹션을 추가한다.
> 각 영역은 **PASS / 발견N / 해당없음** 중 하나. 발견 항목은 그 자리에서 수정 후
> 커밋(다음 일련번호)하고 '조치'에 버전 기록. **9영역 전부 PASS 또는 해당없음**이어야
> 블록 종료 선언. 텍스트 에디터·메모장으로 바로 열람 가능.
>
> `audit_scan.py`(정적 점검 자동 스캐너)는 **첫 종합 감사 실전에서** 손으로 친 Grep을
> 추출해 만든다(추측 선제작 금지 — YAGNI).

---

## 섹션 템플릿 (복사해서 위에 채움)

```markdown
## [YYYY-MM-DD] vXX 블록 종합 감사 — <블록명> (트리거: 1/2/3)
범위: vXX.YY.ZZ ~ vXX.YY.ZZ

| 영역 | 판정 | 발견 항목 | 조치 |
|------|------|-----------|------|
| ① 코드·로직     | PASS/발견N/해당없음 | 상호작용·하위호환·신기능8항목·전역오염·부모무수정·cfg키·UI인덱스·payload | vXX.YY.ZZ |
| ② DB·수치       | PASS/해당없음 | 공개제원 대조·spec_db=DB·normalize·DB키 | — |
| ③ 회귀          | PASS | 결정론(sim_seed·RNG순서)·골든 커버리지 | — |
| ④ 통합 MC+성능  | PASS | 기준값 대조: 요격률 NN%±N (기준 NN%) · 단발 N.Ns/전장 N.Ns (이전 N.Ns) | — |
| ⑤ exe·빌드      | PASS | 번들무결성(datas·hiddenimports)·스모크·워커잔존0·Cesium graceful·_internal | — |
| ⑥ 위생          | PASS/발견N | 코드명·완료항목·죽은코드·상수정합·보안·gitignore·함수표 | — |
| ⑦ 하위호환      | PASS | 구버전 cfg dict 로드·실행 | — |
| ⑧ 수치·단위     | PASS/발견N | NaN/Inf·확률[0,1]clamp·단위혼동(km/m·USD raw↔/1e6)·Beta pk_dist | — |
| ⑨ 리소스 누수   | PASS | frames `if not _mc_mode`·figure close·_log mc가드·히스토리5·QWebEngine | — |

종합 판정: 통과 / 미통과(재감사 필요)
요약: <한 줄>
```

---

<!-- 첫 종합 감사부터 아래에 최신순으로 기록 -->

## [2026-07-08] v19 공군 작전급 블록 종합 감사 — 제공권·SEAD·전략폭격·CAS (트리거: 3 — Claude 추천·사용자 승인, 블록 종결)
범위: v18.01.01 ~ v18.01.06 (v19.1~v19.5), 커밋 fe085ab..HEAD. 무인 모드.

| 영역 | 판정 | 발견 항목 | 조치 |
|------|------|-----------|------|
| ① 코드·로직     | **발견2** | **HIGH: `self.chk_strike` 위젯 이름 충돌** — v19.4 전략폭격 체크박스가 기존 "공격 임무" 위젯(기본 ON)에 재바인딩되어 orphan화 → `enable_strategic_strike`가 항상 True(기본 OFF 계약 위반·3종세트 붕괴). LOW: `air_sorties` 합계에 전략폭격 소티 누락. 나머지 8항목(하위호환·전역오염·부모무수정·cfg키·CAS경계·결정론) PASS | v18.01.07 (위젯 고유명 `chk_strategic_strike` 개명 + air_sorties 합산 정정) |
| ② DB·수치       | 발견1 | AIR_FORCE_DB 9기종 중 KF-21 전투반경 500km가 공개제원(~1000km) 대비 2배 과소·KF-16보다 짧은 서열 역전. 순항속도·소티율·임무역할 8기종 정합 | v18.01.07 (KF-21 combat_radius_km 500→1000, 엔진 미사용 필드=회귀 무영향) |
| ③ 회귀          | PASS | 8×26 bit-identical (공군 OFF·KF-21 정정 후 재확인) | — |
| ④ 통합 MC+성능  | PASS | 공군 전기능 ON n=200: NaN/Inf 0·172ms/run(급증 없음)·승7.5/무75.5/패17·통제 0.442±0.227. 확률값 [0,1] clamp 정상 | — |
| ⑤ exe·빌드      | PASS | 빌드 exit0·GUI 캠페인 스모크 PASS(안개·제공권·방공망·적기지 배너 전부)·감사수정 후 재빌드·재스모크 PASS | — |
| ⑥ 위생          | PASS | 신규 메서드 8종 죽은코드 0·헤더=APP_VERSION=changelog 정합·_PLANS v19.5 완료분 제거·정적 37/37 | — |
| ⑦ 하위호환      | PASS | 공군 OFF: air=None·air 키 부재·구 v18 캠페인 정상 동작 | — |
| ⑧ 수치·단위     | PASS | NaN/Inf 0·확률값 clamp·CAS relief∈[0,0.5]·dmg 음수 불가 | — |
| ⑨ 리소스 누수   | PASS | engine_airforce: _log/print 0·matplotlib 0·frames 0 (MC 과출력·figure·프레임 누수 없음) | — |

종합 판정: **통과** (발견 3건 전부 그 자리 수정·재감사 PASS 후 블록 종료 선언)

메타 회고: ①의 HIGH(위젯 이름 충돌)를 3종세트 정규식 검사가 놓쳤다 — 정규식은 `enable_xxx` 문자열의 build/restore 존재만 보고 "같은 `self.chk_XXX`에 두 플래그가 묶였는지"는 못 본다. → `audit_static_scan.py`에 **`chk_widget_dup` 신설**(QCheckBox 위젯 attr 중복정의=orphan 검출, 뮤테이션으로 chk_strike 충돌 재현·검출 확인, 36→37항목). 이 부류(위젯 attr 재사용)를 앞으로 자동 봉인. 숙제: CAS 발현 강제 GUI 스모크(강한 적 프리셋 UI 조작)는 미구현 — 조건부 검증만.

---

## [2026-07-07] v18 캠페인 블록 종합 감사 — 작전급 캠페인 엔진 (트리거: 1 — v16→v17 major 전환)
범위: v17.01.01 ~ v17.01.08 (직전 v16 종합감사 커밋 c3032b1 이후, 커밋 66f88c8~dbfc546). 핵심 변경: **engine_campaign.py 신설(559줄)** — 1시간 틱 작전급 캠페인, 전술 엔진(engine_combat) **무수정 호출**. app_main.py(+332: SimWorker 캠페인 분기·MC 라우팅·`_render_campaign_result`·`_render_campaign_report` 서브탭). app_main.spec(engine_campaign datas). ai_policy_infer.py(_MEIPASS 절대경로 해석 개선). _audit_campaign_smoke.py 신설·audit_static_scan.py 개선.
범위 주석: v18.1 코어루프·v18.2 전력관리·v18.3 임무배정·v18.4 전장의안개·v18.5 SLOC 연속통제도·v18.6 캠페인 MC·v18.7 분석 리포트. 신규 토글 2종(enable_campaign_mode·enable_campaign_fog, 기본 OFF·실험적). 신규 DB 없음(기존 SHIP_DB·FLEET_PRESETS·ENEMY_FLEET_PRESETS 재사용). 전술 엔진 무변경 → 단발·전장 회귀 bit-identical.
방식: **무인 모드**(사용자 "감사 하자" 승인 = 9영역 포괄 동의). 각 마이너는 개별 code-review(v18.5·18.6·18.7 각 정확성 0건, shift-left)를 이미 거침 → 종합은 engine_campaign 전체 통합 리뷰(에이전트)·상호작용·하위호환 중심. 병합 안 된 연속 커밋이라 code-review 빈 diff → 엔진 파일 통째 에이전트 리뷰로 대체.

| 영역 | 판정 | 발견 항목 | 조치 |
|------|------|-----------|------|
| ① 코드·로직     | 발견1 | 엔진 정밀리뷰(에이전트) 8집중영역 버그0·3종세트(campaign_mode·fog) 무결·전역DB mutate0(.get 읽기전용)·부모무수정(engine_combat 무변경 회귀 bit-identical)·상태머신 이중진입0·cfg=dict(cfg)·rng 결정론. **발견: horizon_h≤0 축퇴→빈 루프로 교전0인데 outcome 'win'** | v17.01.09 `max(1,...)` 클램프 |
| ② DB·수치       | 해당없음 | 신규 무기 제원 없음(기존 DB 재사용). 새 수치는 추상 계수(수리1~14일·탄약0.28/교전·α0.3)=교리적, 공개제원 대조 대상 아님. db_specsheet=DB합 정적 PASS | — |
| ③ 회귀          | PASS | 8×26 bit-identical (전술 엔진 무수정 재확인, 클램프 수정 후 재PASS) | — |
| ④ 통합 MC+성능  | PASS | 캠페인 MC 1000회 ~42s(단발 0.043s)·NaN/Inf 0·outcome 합=1·clamp[0,1]·min≤max | — |
| ⑤ exe·빌드      | PASS | 빌드 성공·번들(engine_campaign datas·_MEIPASS)·단발 스모크 PASS·캠페인 스모크(MC+안개) PASS·abort 전파 OK(재빌드 후 재PASS) | — |
| ⑥ 위생          | 발견1 | 3종세트 무결·죽은코드0(monte_carlo_campaign·_render_campaign_report·run_campaign 호출처 존재)·보안(키 없음)·정적 28/28. **발견: CLAUDE.md 파일구조 표에 engine_campaign.py 누락** | v17.01.09 파일표 추가 |
| ⑦ 하위호환      | PASS | 캠페인 키 없는 구cfg·빈cfg run_campaign 정상·구cfg run_v7/run_battle 정상 | — |
| ⑧ 수치·단위     | PASS | NaN/Inf 가드(target 분모·mean_control·best_control default)·확률[0,1] clamp·비용 /1e6 단위. horizon 클램프(①과 교차) | — |
| ⑨ 리소스 누수   | PASS | 캠페인 frames 미사용(리스트 지표만)·MC가 run 결과 dict 미축적(GC)·figure ChartPageWidget 관리(stop_worker 정리 추가)·엔진 _log/print 없음·model 1회 로드 공유 | — |

종합 판정: **통과** (발견 2건 그 자리 수정 v17.01.09, 9영역 전부 PASS/해당없음)
요약: 캠페인 엔진(engine_campaign 신설)이 전술 엔진 무수정으로 회귀 bit-identical, 에이전트 정밀리뷰 버그0, 발견 2건(horizon 축퇴·CLAUDE.md 파일표)만 경미·즉시 수정.
메타 회고: 이번에 캠페인 3종세트(campaign_mode·fog)를 **수동 확인**해야 했다 — `audit_static_scan.chk_flag_triplet`의 engread가 engine_combat만 봐서 engine_campaign/app_main 소비 플래그를 오탐. → **즉시 도구화**: 소비처 명시(fog=engine_campaign·mode=app_main)한 캠페인 계열 별도 검사 추가(정적 26→28). 다음 새 실행 모드도 소비처 명시 패턴으로 등록. 남은 숙제: 병합 후 code-review 빈 diff(엔진 파일 통째 에이전트 리뷰로 대체 — 이번 확립).

## [2026-07-05] v16.13~16.14 블록 종합 감사 — 지속전장 보급·레이저·즉시예측·직접편성 (트리거: 1 — v16→v17 major 전환 직전)
범위: v16.13.03 ~ v16.14.02 (직전 v16.12~13 종합감사 커밋 904cbb6 이후 15커밋). 핵심 변경: engine_combat.py(RAS 탄약 재보급 `_ras_update`·레이저 DEW `_laser_defense`+SHIP_POWER 전력모델·전장 전용)·app_main.py(온보딩 배너·해석 배너·반사실 토글영향 워커·예상전황 학습 대리모델·아군 직접 편성 다이얼로그)·engine_core.py(SHIP_POWER DB 신설).
범위 주석: v16.13.03~04 온보딩·해석배너(UI)·05 RAS 재보급(엔진, 전장 전용)·06 토글 실제영향(UI 워커)·07 위생·08~09 레이저 DEW(엔진)·10 레이저 종결(음성)·11~12 기뢰전·USV 승격(레이블)·14.01 즉시예측(표시·surrogate)·14.02 직접편성(UI). 신규 토글 2종(enable_ras_rearm·enable_laser_dew) + 신규 stats 1종(laser_kills) + 신규 DB(SHIP_POWER). DB 기본동작 변경 없음(레이저·RAS 기본 OFF).
방식: **무인 모드**(사용자 "감사 하고나서" 승인 = 9영역 포괄 동의). 각 마이너는 개별 code-review(RAS medium·레이저 high)·정적스캔·회귀를 이미 거침(shift-left) → 종합은 누적 상호작용·통합 상태·하위호환 중심.

| 영역 | 판정 | 발견 항목 | 조치 |
|------|------|-----------|------|
| ① 코드·로직     | PASS | 신기능 체크리스트: 3종세트 무결(enable_ras_rearm·enable_laser_dew 체크박스+cfg빌드+cfg로드)·신규 stats laser_kills MC 3경로 완비(monte_carlo_v7·_mc_batch_worker·monte_carlo_lhs)·db_specsheet 정합(정적)·import 빌드검증·부모무수정(OFF bit-identical 회귀PASS) | — |
| ② DB·수치       | PASS | SHIP_POWER 전력값 현실적(KDX-III 발전 9MW급=가스터빈함 정합·레이저 5km HELIOS 근거 주석·B2만 100kW 탑재). 승격 2종은 레이블만·즉시예측/직접편성 DB 무변경 | — |
| ③ 회귀          | PASS | 8×26 bit-identical (엔진 신규는 전부 기본 OFF·전장 전용) | — |
| ④ 통합 MC+성능  | PASS | 신규 토글 조합(레이저+RAS+무인+DMO+기뢰+직접편성) 전장 MC n=20: NaN0·win/draw/loss=0.65/0.2/0.15(합1.0)·범위[0,1]OK · 전장 5.0s/run 정상(단발~45배=1800틱)·급증 없음 · laser_kills=0은 레이저 음성 결론 일치 | — |
| ⑤ exe·빌드      | PASS | 빌드 성공(v16.14.02) · GUI 스모크 RESULT_CODE=0(직접편성 버튼→다이얼로그→담기×2→확정→'적용 중'레이블→시뮬→요격률 전 경로) · 워커 잔존0(closeEvent) | — |
| ⑥ 위생          | PASS | 정적 25/25(신규 3종세트 2개 반영 후) · 완료 설계문서 2종 _archive/plans/ · README forecast_features.py 기재·단계 v16.14 · changelog·변경이력 정합 | v16.14.02(위생 포함) |
| ⑦ 하위호환      | PASS | 구버전 cfg(신규 플래그·fleet_custom 전부 없음)로 단발(요격률 0.32)·전장(outcome loss·score 0.001) 정상·NaN0·laser_kills 기본0 | — |
| ⑧ 수치·단위     | PASS | 레이저 dwell `max(dist_m,1.0)` 0나눗셈 가드·도달출력 min상한·거리>사거리 리셋 · forecast clip[0,1]·expm1 역변환·cpw win>0 가드 · 직접편성 정수 sum | — |
| ⑨ 리소스 누수   | PASS | 신규 워커(ShowcaseCompare·Counterfactual) closeEvent 중단 정리·figure plt.close·FleetCustomDialog 모달 자동정리(QThread 아님) | — |

종합 판정: **통과** (9영역 전부 PASS·발견 0건)
요약: 지속전장 보급·레이저·즉시예측·직접편성 15커밋 통합 상태 무결. OFF bit-identical·조합 NaN0·하위호환·GUI 스모크 전 경로 PASS.
메타 회고: 정적스캔 3종세트 자동검사에 이번 블록 신규 토글 enable_ras_rearm·enable_laser_dew가 빠져 있어 ①에서 수동 확인 → **즉시 audit_static_scan.py에 2개 추가(23→25 검사)**. 다음 숙제(지속): 병합/커밋열 감사에서 `/code-review` 타깃 빈 diff(shift-left 개별리뷰로 대체 중)·GUI abort 클릭 미자동화. [[feedback-audit-self-improve]].

## [2026-07-03] v16.12~16.13 블록 종합 감사 — 무인·자율 자산(트랙 A·B·C) + 쇼케이스 (트리거: 1 — v16→v17 major 전환 직전)
범위: v16.12.01 ~ v16.13.02 (직전 v16.11.02 종합감사 커밋 9aad354 이후 7커밋). 핵심 변경: engine_combat.py(자율교전·CEC 지휘저하·정찰/무인 함정 로직)·app_main.py(쇼케이스 탭·신규 토글 3종·cfg 빌드 리팩터)·engine_core.py(신규 DB: 정찰 드론 2종·USV/UUV·자폭 드론 군집).
범위 주석: 트랙 A-1 정찰 드론 ISR(v16.12.01)·A-2 무인 함정 USV/UUV(v16.12.02)·B 적 무인기 군집(v16.12.03)·쇼케이스 탭(v16.13.01)·C 함정 자율 교전(v16.13.02). 신규 토글 3종(enable_recon_drone·unmanned_assets·autonomous_engagement) + enable_drone_swarm(v16.12.03), 신규 stats 2종(recon_losses·unmanned_lost), **기본 동작 변경 1종(기함 격침 시 CEC 지휘 저하 — 골든 갱신)**.
방식: **무인 모드**(사용자 "추천대로" 승인 = 9영역 포괄 동의). 각 마이너·트랙은 개별 code-review(트랙 C는 high)·정적스캔·회귀를 이미 거침(shift-left) → 종합 감사는 누적 상호작용·통합 상태·하위호환 점검 중심.

| 영역 | 판정 | 발견 항목 | 조치 |
|------|------|-----------|------|
| ① 코드·로직     | PASS | autonomous×타 신기능 8종 조합 스윕 전부 NaN/범위 OK·3종세트(autonomous·unmanned)·MC 3경로(recon_losses·unmanned_lost)·shift-left 개별리뷰(트랙 C high) | — |
| ② DB·수치       | PASS | 신규 제원 현실적(Shahed급 50m/s·송골매 42m/s·시가디언 90m/s·자폭 USV 14m/s) — 각 트랙 baseline서 공개제원 대조 완료·db_specsheet 정합(정적) | — |
| ③ 회귀          | PASS | 8×26 bit-identical (CEC 지휘저하 골든 갱신: 랴오닝-기본#3만 변경, 의도된 기본동작) | — |
| ④ 통합 MC+성능  | PASS | 조합 스윕 NaN0 · 기준 단발 0.21s (자율교전 경량 조건 추가로 성능 급증 없음) | — |
| ⑤ exe·빌드      | PASS | 빌드 EXIT0 · GUI 스모크 RESULT_CODE=0 (쇼케이스 페이지 빌드 포함) | — |
| ⑥ 위생          | PASS | 정적 23/23 · 완료 plan 2종 _archive/plans/ 이동 · _PLANS v15.3 삭제 · README 단계 v16.13 · changelog·변경이력 정합 | — |
| ⑦ 하위호환      | PASS | 신규 플래그 0개 구버전 cfg dict 로드·실행 정상 (요격률 16.7%·범위 OK) | — |
| ⑧ 수치·단위     | PASS | 조합 스윕 NaN/Inf/범위오류 0 · 자율교전 신규 나눗셈 없음(시간비교·정수 salvo) | — |
| ⑨ 리소스 누수   | PASS | 자율교전 _log `if not _mc_mode` 가드 · 쇼케이스 워커 closeEvent 정리·중복실행방지 · figure 무관(라벨만) | — |

종합 판정: **통과** · 발견 0건
요약: 무인·자율 4블록(트랙 A·B·C·쇼케이스) 통합 상태 9영역 전부 PASS. 트랙 C의 CEC 지휘 저하(기본 동작 변경)가 cec_jammed·DMO·다방위 등과 조합해도 안전. v15 무인·자율 로드맵 소진 → v17 진입 준비 완료.
메타 회고: `enable_autonomous_engagement`·`unmanned_assets`가 정적 스캔 3종세트 자동검사에 없어 수동 확인 → 스캐너 목록에 추가(21→23항목). recon_drone은 `_restore_cfg` for-루프 복원이라 개별 setChecked 정규식과 불일치(오탐) → 주석으로 제외 명시. 숙제: for-루프 복원 패턴도 인식하는 restore 검사 일반화(다음 감사).

## [2026-06-30] v16 블록 종합 감사 — 전장 도메인 확장 (DMO·C-RAM·전자좌표기만·기뢰전·항만방어) (트리거: 1 — v16→v17 major 전환 직전)
범위: v16.06.01 ~ v16.11.02 (직전 v16.05 감사 커밋 756840e 이후 9커밋). 핵심 변경: engine_combat.py +173·engine_core.py +110·app_main.py +121·db_specsheet.py +112.
범위 주석: 신기능 5종(분산해양작전 DMO·해안 C-RAM/SAM 포대·전자 좌표 기만·기뢰전 MIW·항만 거점 방어[자폭 무인수상정 USV]) + 적 밸런스 2종(항모 킬 체인·전면전 포화 규모화). 신규 토글 4종(enable_dmo·coord_deception·mine_threat·minesweeping), 신규 stats 2종(mines_struck·ships_lost_to_mine), 신규 적 위협 1종(자폭 USV).
방식: **무인 모드**(사용자 "종합 감사 가자" 승인 = 9영역 포괄 동의). 각 마이너(v16.07~16.11)는 개별 code-review·정적스캔을 이미 거침(shift-left) → 종합 감사는 누적 상호작용·통합 상태 점검 중심.

| 영역 | 판정 | 발견 항목 | 조치 |
|------|------|-----------|------|
| ① 코드·로직     | PASS | 신기능8항목: enable 3종세트 4종 무결·신규 stats MC 3경로(monte_carlo_v7·_mc_batch_worker·monte_carlo_lhs) 전부 집계·normalize is_suicide setdefault·db_specsheet 동기화. 전역 DB mutate 0(ENEMY/FRIENDLY/SHIP)·cfg=dict(cfg) 유지·부모무수정(`_pick_target` threat_pos 기본인자 추가·BattleEngine 오버라이드 없음)·OFF no-op=회귀 bit-identical. 죽은코드 0 | — |
| ② DB·수치       | PASS | db_specsheet 112 = 엔티티 DB 합(ENEMY68+FRIENDLY14+SHIP23+AIRCRAFT7)·누락 0. 신규 자폭 USV 수치(speed 14m/s·hp1·rcs5) 자폭 무인보트로 합리적. 적편대 교리규모(v16.05~06 검증분)·DB키 일관(항만 침투 복합 preset 키 전부 ENEMY_DB 존재) | — |
| ③ 회귀          | PASS | 8케이스×26지표 bit-identical(신규 토글 전부 OFF no-op·골든 무영향). is_suicide setdefault·_pick_target 기본인자가 RNG 순서 미변경 | — |
| ④ 통합 MC+성능  | PASS | 이지스 기동전단 vs 입체포화 MC40 mean_intercept 0.785±0.067·VLS소진율 0.925·NaN 0·MC40 wall 14.0s. 단발 wall 입체포화/항만침투복합 정상(<수초)·이전 블록 대비 급증 없음 | — |
| ⑤ exe·빌드      | PASS | 빌드 exit0·GUI 스모크 RESULT_CODE=0(홈→앱→시뮬→요격률 표시→정상종료)·번들무결(스모크 간접). MC 워커풀·abort 경로 무변경(Job Object 구조해결 유지) | — |
| ⑥ 위생          | 발견2 | (1) 구현완료 plan_v16_4~v16_8 5개 루트 잔존 → _archive/plans/ 이동. (2) DMO·전자좌표기만 검증완료인데 '실험적' 레이블 stale → 정규 승격(레이블 제거, 기본 OFF 유지). _PLANS v16.4~8 미래형 잔류 0·정적 19/19 | v16.11.02 |
| ⑦ 하위호환      | PASS | 구버전 cfg(신규 토글 4종 전부 누락 dict)로 run_v7_simulation 정상 실행·intercept_rate 0.095·NaN 0 | — |
| ⑧ 수치·단위     | PASS | 전 신규 토글 ON 조합(DMO+coord_decep+mine+sweep 동시) 실행 OK·intercept_rate 0.686·NaN/Inf 0·확률[0,1] clamp 위반 0 | — |
| ⑨ 리소스 누수   | PASS | 기뢰 `_log`(_apply_mine_exposure) `if not self._mc_mode:` 가드 내부·이번 블록 frames/figure 누적 변경 없음 | — |

종합 판정: **통과**
요약: v16 블록 9영역 전부 통과. 발견 2건(완료 plan 아카이브·DMO/좌표기만 승격) 그 자리 조치(v16.11.02). 판단필요 0건. v16.x 도메인 소진 — v17(군수·미래전장) 진입 준비 완료.
메타 회고: ▸병합 후 `/code-review` 빈 diff는 각 마이너 shift-left(개별 리뷰)로 대체 — 알려진 빈틈 유지. ▸**개선 숙제**: 구현완료 `plan_v*` 루트 잔존을 `audit_static_scan.py`에 자동검사 추가(이번엔 수동 발견 — changelog 구현된 minor의 plan 파일이 _archive 아닌 루트에 있으면 경고). ▸abort GUI 시나리오는 워커풀 무변경이라 생략(여전히 `_audit_gui_smoke.py`에 abort 미구현, 지속 숙제).

## [2026-06-28] v16.05 블록 종합 감사 — 함정 재고·편성 현실화 + 적 포화 증강 (트리거: 1 — 재고·편성 현실화 묶음 ✅일단락)
범위: v16.05.01 ~ v16.05.05 (직전 v16 2차 감사 커밋 0848ad7 이후 3 작업커밋 + 감사규칙 보강 fe5d30d + 본 감사 발견수정 v16.05.05). 변경 파일: engine_core.py(DB 65줄)·audit_regression_golden.json(골든 58줄)·app_main.py(헤더·버전)·app_changelog.json·CLAUDE.md(감사규칙 2줄). **engine_combat.py 무변경 = 교전 로직 변화 0**.
범위 주석: DB 수치·편성 변경 위주 블록. 아군 VLS 셀 준수(KDX-III-B2 정조대왕함 SM류 112→48·미 DDG-51 SM류 112→72·CG-47 140→104·ESSM quad-pack 반영)·한국 호위함 SM-2 미탑재 정정(FFX-I/II/III)·적 포화 교리화(SEAD ARM 12→24·A2/AD 항공 6→10기). 신규 토글·신규 클래스 없음.
방식: **무인 모드**(사용자 "종합 감사 한번 하자" 승인 = 9영역 포괄동의). audit_verify_regression.py·audit_static_scan.py·_audit_gui_smoke.py 활용, ④ 통합 MC는 엔진 호출 측정.
메타: 빌드 exit0·GUI 스모크 RESULT_CODE=0·잔존 프로세스 0 · 무인 100%(개입 0회)·재감사 0회(②발견 수정 후 회귀 재PASS) · 점검규모 회귀 8×26·정적 19/19·MC60×3시나리오 · 환경 Python 3.14·Win11.

| 영역 | 판정 | 발견 항목 | 조치 |
|------|------|-----------|------|
| ① 코드·로직     | **PASS** | engine_combat.py 무변경(교전 로직 0)·app_main.py는 헤더·APP_VERSION만. 신규 enable_xxx·신규 클래스·신규 stats 키 0 → 3종세트·MC 3경로 영향 없음(정적 19/19). 변경은 전부 DB dict 값 | — |
| ② DB·수치       | **발견1** | **VLS 셀 준수 정합**: KDX-III-B2 Mk.41 48셀=SM-3 16+SM-6 12+SM-2 20 정확 / DDG-51 96셀=SM류 72+ESSM quad 8셀+토마호크 8=88≤96 정확 / CG-47 122셀=SM류 104+ESSM quad 6셀=110≤122(토마호크 16은 inventory 외·주석 '126셀급' 자인, 시뮬 무영향=경미·비채택). FFX-I/II/III SM-2 제거 현실 정합(인천급 RAM만·대구/충남급 해궁만). **발견1**: SEAD 프리셋 ARM 합이 changelog·헤더·주석 3곳 표기(24발)와 달리 실제 20발(Kh-31P 10+LD-10 6+Kh-58U 4) | **v16.05.05** ARM 12+8+4=24발로 정정(비율 3:2:1 유지) |
| ③ 회귀          | **PASS** | 8케이스×26지표 골든 일치(수정 전·후 2회 PASS). 적 증강·미함 재고는 골든 편대 미포함이라 무영향, 아군 호위함/KDX-III 골든은 갱신분 일치 | — |
| ④ 통합 MC+성능  | **PASS** | 입체포화/SEAD/A2AD MC60 각각 **NaN/Inf 0** · SEAD 24발: 요격률 0.971±0.030·SM-3/SM-6 소진율 **1.0**·VLS 고갈 0.175(재고 현실화가 요격탄 소진을 실제로 유발=의도 효과) · wall-time 정상(단발 0.43s·MC60 21~56s, 급증 없음) | — |
| ⑤ exe·빌드      | **PASS** | 빌드 exit0 · **GUI 스모크 PASS**(홈→앱→시뮬→요격률 표시→정상종료, RESULT_CODE=0) · 종료 후 잔존 프로세스 0 · DB만 변경이라 번들 데이터 무변경 | — |
| ⑥ 위생          | **PASS** | 정적 19/19(버전정합 v16.05.05·_PLANS stale 0·db_specsheet 정합·파일명 stale 0) · changelog 연속(v16.05.01~05)·헤더·APP_VERSION 정합 · ②발견 수정으로 표기-실제 일치화 · 죽은코드 0 | — |
| ⑦ 하위호환      | **PASS** | 신규 키 없는 구버전 cfg dict로 단발 정상 실행(요격률 0.095 반환). DB 키 구조 무변경 → 저장 시나리오 호환 | — |
| ⑧ 수치·단위     | **PASS** | DB 값 전부 정수 재고·셀 수(단위 혼동 무관) · MC 결과 NaN/Inf 0 · 확률값(요격률·소진율) 구조적 [0,1] | — |
| ⑨ 리소스 누수   | **해당없음** | 엔진·시각화 무변경(DB dict 값만) → frames/figure/_log 경로 무관 | — |

종합 판정: **통과** (9영역 PASS/해당없음·발견 1건(②ARM 표기-실제 불일치)→v16.05.05 그 자리 수정·회귀 재PASS)
요약: 재고·편성 현실화 블록 무결. VLS 셀 준수·호위함 무장 정정 전부 공개제원 정합, 적 포화 증강이 SM-3/SM-6 완전 소진을 유발해 신기능 발현 지렛대(적 위협 증강)가 수치로 입증됨. engine_combat 무변경으로 회귀·하위호환 완전 보존.
메타 회고: ▸**즉시 반영** — audit_static_scan.py가 cp949 콘솔에서 '—'(em-dash) 출력 시 UnicodeEncodeError로 깨짐 → 스크립트 진입부에 stdout utf-8 재구성 추가(수동 PYTHONIOENCODING 의존 제거). ▸**숙제** — 프리셋 편성 count 합과 changelog/헤더/주석 표기 수치의 정합을 자동 검사하지 못해(이번 ARM 20 vs 24 발견은 수동 산술) 수동으로 잡음 → 프리셋 합↔표기 정합 검사는 자유텍스트 파싱이라 난도 높음, [[patch-queue]]에 '감사 개선' 숙제로 등록.

## [2026-06-25] v16 블록 2차 종합 감사 — 사이버전·극초음속 활공·대잠 접촉단절 (트리거: 1 — v16.1~v16.3 기능 묶음 ✅완성)
범위: v16.02.04 ~ v16.04.04 (직전 v16 감사 커밋 425f378 이후 6커밋). 핵심 변경 engine_combat.py +142줄·engine_core.py +7줄(극초음속 편대)·app_main.py(체크박스 2종·헤더·_PLANS)·audit_static_scan.py(+2 플래그·chk_plans_stale 오탐수정).
범위 주석: 신규 토글 2종 — `enable_cyber_warfare`(사이버전 3채널 확률 침투)·`enable_hgv_glide`(극초음속 활공 궤적 동적 고도). `enable_sonar_emcon`은 기존 토글에 접촉 단절 구조 추가(능동 핑 노출→회피 잠항). 전부 기본 OFF·실험적. 신규 시나리오 '극초음속 포화 공격'(DF-17 12+YJ-21 8).
방식: **무인 모드**(사용자 "종합 감사부터 하자" 승인 = 9영역 포괄동의). ①코드는 백그라운드 에이전트(general-purpose, git diff 425f378..HEAD 누적 diff 직접 분석). audit_verify_regression.py·audit_static_scan.py·_audit_gui_smoke.py 활용.

| 영역 | 판정 | 발견 항목 | 조치 |
|------|------|-----------|------|
| ① 코드·로직     | 발견1 | 에이전트 누적diff 분석: 치명/높음/중간 0건. 플래그 3종 OFF no-op(random 미소비) 실증·SoA `_et_contact_lost_until` 3곳 정합·부모 무시그니처변경·3기능 동시 ON 충돌 없음. **낮음1**: `_hgv_glide_alt` 정점<2km 활공체 종말 고도 역전(현 DB 미발현) | v16.04.04에서 종말고도 min(2km,start) 클램프 수정·재감사 PASS |
| ② DB·수치       | PASS | DF-17 마하8.7·2500km·60km / YJ-21 마하9.9·1500km·40km 공개제원 정합. db_specsheet 둘 다 정합(편성만 신규, 제원 무변경). 극초음속 포화 20발=최대규모 일제로 타당 | — |
| ③ 회귀          | PASS | 8케이스×26지표 골든 일치. 신규 random(사이버 침투·HGV·핑회피)은 전부 플래그 ON 경로 한정 → OFF bit-identical | — |
| ④ 통합 MC+성능  | PASS | 입체포화 100회 NaN/범위이상 0건. v16 신기능 순수 오버헤드 1.18배(230→272ms, 1.5배 미만). 첫 측정 1.64배는 sonar_equation(기존 환경물리) 포함 불공정 → 규명 완료 | — |
| ⑤ exe·빌드      | PASS | 빌드 exit0·GUI 스모크 PASS(시뮬→요격률, 첫 BLOCKED는 앱진입 타이밍·재시도 PASS)·번들 무결(새 데이터파일 없음·spec 무변경)·MC abort 경로 미변경 | — |
| ⑥ 위생          | PASS | 정적 19/19(chk_plans_stale 오탐수정 포함)·_PLANS v16.2/16.3 삭제 확인·plan_v16_2 구현완료→_archive/plans 이동·죽은코드 없음·상수 정합 | plan 아카이브 이동 |
| ⑦ 하위호환      | PASS | 신규 키 없는 구버전 cfg dict로 단발·전장 정상 실행(NaN 없음)·3종세트 완비 | — |
| ⑧ 수치·단위     | PASS | `_hgv_glide_alt` 경계 15케이스 유한·비음수·사이버 계수 전부[0,1]·회피점프 좌표 NaN 불가·확률값 clamp | — |
| ⑨ 리소스 누수   | PASS | 신규 메서드 _log/frames 무관(상태계산만)·접촉단절 로그 2곳 mc_mode 가드 완비 | — |

종합 판정: 통과
요약: 9영역 PASS(①낮음1 v16.04.04 수정). 누적 diff 에이전트 치명/높음/중간 0건, 3토글 OFF bit-identical 실증, 신기능 오버헤드 1.18배. 메타회고: chk_plans_stale이 APP_VERSION seq↔로드맵번호 충돌(v16.04 vs _PLANS v16.4) 오탐 → 제목 내용 비교로 차단(즉시 반영).

## [2026-06-25] v16 블록 종합 감사 — 전자전·대잠 EMCON·극초음속 다층요격 (트리거: 1 — v16.1·v16.2 기능 묶음 ✅완성, 새 작업 전 매듭)
범위: v16.01.01 ~ v16.02.03 (직전 self-play 감사 커밋 62e54de 이후 13커밋). 실 코드 변경은 engine_combat.py·engine_core.py·app_main.py 3개 파일에 국한 — 나머지 커밋은 파일명 리네임(33개 하위시스템 접두 체계)·DB 검증 2차·위생·감사 자동화 보강·README 문서화.
범위 주석: 신규 기능 3종 토글 — `enable_esm_arm`(ESM→ARM 역탐지)·`enable_sonar_emcon`(능동 소나 핑 역탐지)·`enable_asw_forward`(대잠 항공 전진 초계). 전부 기본 OFF·실험적. v16.02.01 극초음속 다층요격(SM-6 Block IB 저고도/SM-3 고고도)은 DB 추가 없이 `_select_defense_wpn` 분기만 확장.
방식: **무인 모드**(사용자 "마무리 전부" 승인 = 9영역 포괄동의) — 단계별 동의 없이 자동 수행. ①코드·②DB는 백그라운드 에이전트 팬아웃(코드 general-purpose·DB Explore+WebSearch), ⑤ exe 스모크는 GUI 자동화(pywinauto, RESULT_CODE=0). audit_verify_regression.py·audit_static_scan.py·_audit_gui_smoke.py 활용.
메타: 빌드 exit0(2회: 스모크용+주석수정 반영)·GUI 스모크 RESULT_CODE=0·잔존 프로세스 0 · 무인 100%(개입 0회)·재감사 0회 · 점검규모 회귀 8×26·정적스캔 14/14·① 누적diff 에이전트·② DB 현실성 에이전트 · 환경 HEAD ab98437·Python 3.14·Win11.

| 영역 | 판정 | 발견 항목 | 조치 |
|------|------|-----------|------|
| ① 코드·로직     | **PASS** | 누적 diff(62e54de..HEAD) 에이전트 리뷰 — 치명/높음/중간 **0건**. OFF 경로 3종 검증된 no-op(`_arm_esm_update` 즉시 return·`_evading` 우변 False·transit 분기 `if self._asw_forward` 가드) → 회귀 PASS 정합 · 전역 DB mutate 0(ARM `info.copy()`)·부모 무수정(BattleEngine radar_off_until 재사용)·cfg 3키 빌드/로드/엔진읽기 문자열 일치·SoA `_et_counter_evade_until` 인덱스 정합·신규 클래스 0(`_id_counter` 무관)·죽은코드 0 · **신기능 3종세트 완비**(chk 6201/10/19 + isChecked 7933~35 + cfg.get 5664~68) · **낮음 1건**: `_asw_phase` 주석에 v16.01.04 `'transit'` 상태 누락 | 이번 감사 위생 커밋에서 주석 수정 |
| ② DB·수치       | **PASS** | DB 현실성 에이전트 팬아웃(WebSearch 대조) — 차단성 이상치(아음속↔초음속·비전투함 무장) **0건**. 핵심 신규값 정합(랴오닝24/산둥36 함재기·DF-17 60km 활공·ARM Kh-31P/LD-10 사거리·ENEMY_MUNITION 탑재량·마하 환산). 에이전트 권고 4건은 코드 재검증 후 **전부 비채택**: ▸YJ-91 680m/s=과거 LOW-1 검증값(같은 Kh-31 계열 자기모순) ▸푸젠 40+=미취역 추정·specsheet 정합 ▸지르콘/킨잘 고정고도=_PLANS v16.2 후속과제 등록필 ▸Kh-58U=선택적 보강 · db_specsheet 항목수=DB 합(정적스캔 PASS)·편대키 정합 | 권고 비채택(근거 명시) |
| ③ 회귀          | **PASS** | 8케이스×26지표 골든 일치(동작 보존) · 결정론 유지(토글 OFF 기본이라 RNG 순서 불변, OFF bit-identical) | — |
| ④ 통합 MC+성능  | **PASS** | 기준 시나리오(이지스 기동전단 vs 전면전 포화·맑음주간) MC120: 요격률 **62.9%±9.6%**(범위 0.441~0.868)·**NaN/Inf 0** · wall-time 정상(단발 0.25s·전장 0.7s·MC120 5.4s, 이전 블록 대비 급증 없음). 토글 OFF라 회귀 PASS가 안정성 담보 | — |
| ⑤ exe·빌드      | **PASS** | 빌드 exit0 · **번들무결성 OK**(_internal에 ai_rl_policy.npz·changelog·db_specsheet·forecast_surrogate.json·view_cesium_3d.html 포함) · **GUI 스모크 PASS**(홈→앱→시뮬→요격률 표시→정상종료, RESULT_CODE=0) · 종료 후 잔존 프로세스 0 | — |
| ⑥ 위생          | **PASS** | 정적스캔 14/14 PASS(버전정합 v16.02.03·gitignore·민감산출물 0·_log/_record_frame 가드·3종세트·db_specsheet 정합·_PLANS stale 0·README 전수커버·파일명 stale버전 0) · _PLANS v16.1 측정결론 반영필(ab98437) · 죽은코드 0 | — |
| ⑦ 하위호환      | **PASS** | v16 플래그(esm_arm·sonar_emcon·asw_forward·munition_limit) 전무한 구버전 cfg로 단발·전장 둘 다 정상 dict 반환(`cfg.get(...,False)` 폴백) | — |
| ⑧ 수치·단위     | **PASS** | v16 신규 산술 안전: ARM stale Pk `pk_base*exp(-miss_d/150)`=상수분모·결과 자연 [0,1] · `_sonar_eq_pd` r50<0/==0 continue 가드·best_pd 0.0 초기화 → NaN/Inf 없음 · 확률값 clamp 불필요(구조적 [0,1]) | — |
| ⑨ 리소스 누수   | **PASS** | v16 frames/figure 신규 추가 0(시각화 무변경) · `_record_frame`·`_log` `_mc_mode` 가드 정적스캔 PASS · 능동소나 역탐지 cb는 틱당 즉시반환·누적상태 없음 | — |

종합 판정: **통과** (9영역 PASS·발견 1건(낮음 주석)→그 자리 수정·재감사 불요)
요약: v16 전자전·대잠 EMCON·극초음속 블록 무결. OFF 기본 토글이라 하위호환·회귀 완전 보존, 신규 산술 NaN-free. ②DB 에이전트 권고는 코드 재검증으로 전부 비채택(맹목 수용 차단).
메타 회고: ▸**즉시 반영** — v16 신규 3종 플래그(esm_arm·sonar_emcon·asw_forward)가 audit_static_scan.py의 3종세트 자동검사에 빠져 수동 Grep으로 확인했음 → 스캐너에 추가해 다음 감사 자동화(이번 감사 중 반영). ▸**에이전트 전제 오류** — DB 에이전트가 'ARM standalone·carrier_air_wing이 v16 신규'라 잘못 전제(둘 다 기존, v16은 편대 프리셋만 추가). 에이전트에 정확한 diff 범위를 더 명시적으로 줄 것. ▸**숙제** — DB 현실성 에이전트의 권고가 과거 검증결정과 충돌할 때 자동 판별 어려움(YJ-91처럼). 변경 이력 주석(LOW-N)을 에이전트가 먼저 읽도록 프롬프트에 코드 인용 의무화 검토.

## [2026-06-24] self-play/RL 블록 종합 감사 — AI 전술 exe 통합 + 적 전술 RL 주입(self-play) (트리거: 1 — self-play 큰 묶음 ✅일단락)
범위: v15.14.01 + Phase 5.6.1~5.6.3 (직전 v15 감사 커밋 3a78e41 이후 누적 / engine_v7 +19줄·launcher +43줄·launcher.spec +3줄·changelog +8줄. RL 자가개선 S1~S4·Tier2·selfplay_env/loop는 **빌드제외 인프라**(exe 무관) → ⑤ 외 영역에서 엔진 접점만 평가)
범위 주석: exe 노출 변경은 단 둘 — ①v15.14.01(AI 전술 토글, 학습 PPO 정책 numpy 추론을 지속전장에 탑재) ②Phase 5.6.1(BattleEngine에 적 전술 RL 주입 훅 `_enemy_rl`). 나머지 self-play(5.6.2 EnemyEnv·5.6.3 교대 공진화 루프)·자가개선 루프는 빌드제외 .py 도구라 ⑤·⑥(gitignore)에서만 다룸. 가벼운 종합감사(다수 영역 소변경/해당없음).
방식: **무인 모드**(사용자 착수 승인 = 9영역 포괄동의) — 단계별 동의 없이 자동 수행, exe 스모크는 GUI 자동화(pywinauto). audit_scan.py·_audit_smoke.py·_smoke_rl.py 활용.
메타: 빌드 exit0(~70초)·기본 GUI스모크 RESULT_CODE=0·RL토글 GUI스모크 RESULT_CODE=0·잔존 프로세스 0 · 무인 100%(개입 0회)·재감사 0회 · 점검규모 회귀 8×26·audit_scan 11/11·① 누적diff 에이전트 리뷰 1회 · 환경 HEAD 5380718·Python 3.14·Win11.

| 영역 | 판정 | 발견 항목 | 조치 |
|------|------|-----------|------|
| ① 코드·로직     | **PASS** | 누적 diff(3a78e41..HEAD) 에이전트 리뷰 **발견 0건** · 부모 무수정(BattleEngine `_enemy_rl`/`_apply_tactical_choice` enemy_mode 주입/`_adaptive_tactic_update` 오버라이드 모두 self 인스턴스 속성만·super() 위임, TimeStepEngine 시그니처 불변) · `ai_tactic='rl'`은 학습 전용(exe 콤보 미산출)→production 도달불가 dead path · `choice.get('enemy_mode')` None 가드·`_enemy_rl` 기본 False 경로 RNG 불변 · enable_rl_policy 3종세트 완비(체크박스6159+isChecked7873+hasattr5639) · SimWorker rl_infer 라우팅 try/except None 폴백 안전 · 전역 DB mutate 0 | — |
| ② DB·수치       | **해당없음** | DB 수치 변경 0건(rl_policy.npz는 학습 가중치, DB 아님) · spec_db=엔티티DB합 정합 audit_scan PASS 유지 | — |
| ③ 회귀          | **PASS** | 8케이스×26지표 골든 일치(동작 보존) · 결정론 유지(enemy_mode 훅이 단발·기본 경로 RNG 순서 안 건드림, `_enemy_rl` 기본 False) | — |
| ④ 통합 MC+성능  | **해당없음** | 엔진 교전 로직 무변경(훅·추론 주입만, 토글 기본 OFF) → baseline·wall-time 영향 없음. [[project-baseline-battle]] 53.2% 기준 불변 | — |
| ⑤ exe·빌드      | **PASS** | 빌드 exit0(exe 53MB) · **번들무결성 OK**(_internal에 rl_policy.npz·changelog·spec_db·battle_surrogate·cesium, rl_infer hiddenimports 추가) · **기본 GUI 스모크 PASS**(홈→앱→시뮬→요격률 표시→정상종료, RESULT_CODE=0) · **RL 토글 GUI 스모크 PASS**(전장+AI 전술 둘 다 ON→시뮬→임무점수·요격률 표시→정상종료, RESULT_CODE=0) · 종료 후 잔존 프로세스 0 | — |
| ⑥ 위생          | **PASS** | APP_VERSION=헤더=changelog 마지막 v15.14.01 정합(audit_scan) · changelog 연속(v15.13.05→v15.14.01, RL 내부라벨 v15.11~13 충돌 회피 점프 의도적) · gitignore 커버 완전(`_selfplay_*`.zip/.log/.npz·`_rl_ppo_model*`·`_rl_ckpt/`·`_improve_*`) · 민감 산출물 추적 0(추적된 `_rl_*`는 전부 .py 도구) · self-play 소스 10종 .py 정상 추적 · _PLANS 코드명 잔류 0 | — |
| ⑦ 하위호환      | **PASS** | enable_rl_policy·ai_tactic 키 누락한 구버전 cfg로 전장 실행 → win 정상(`cfg.get(...,False)`·`_enemy_rl` 기본 False 폴백) | — |
| ⑧ 수치·단위     | **PASS** | rl_infer.featurize 가드 완비: `dists=[...] or [999.0]`(빈 위협 시 min/len 안전)·`fleet_max=float(..) or 1.0`(0나눗셈 가드)·`irate=.. if tot else 0.0`·horizon 폴백 BATTLE_HORIZON_S>0 · numpy forward 나눗셈 외 log/sqrt 없음 | — |
| ⑨ 리소스 누수   | **PASS** | `_log()` `_mc_mode` 가드·`_record_frame` `if not _mc_mode` 가드 audit_scan PASS · 추론 cb는 30초 주기 호출·누적 상태 없음(numpy forward 즉시 반환) | — |

종합 판정: **통과** (9영역 전부 PASS 또는 해당없음, 발견 0건)
요약: self-play/RL 블록(v15.14.01 AI 전술 exe 통합 + Phase 5.6.1~3 적 전술 RL 주입·공진화 루프) 무인 종합 감사 — 엔진 훅 부모 무수정·`_enemy_rl` 기본 False 회귀 보존·enable_rl_policy 3종세트·rl_infer 폴백/수치 가드·번들 무결성·기본+RL토글 GUI 스모크 전부 클린. self-play 인프라는 빌드제외라 ⑤ 외 다수 영역 소변경/해당없음(예상대로 가벼운 감사).
메타 회고: 약점 2건 → ▸**병합 후 `/code-review` 빈 diff 빈틈을 이번엔 해소** — 누적 diff(3a78e41..HEAD)를 에이전트에 직접 투입해 의미 리뷰 수행(v15 감사 메타회고 숙제 이행, shift-left 대안). ▸**RL 토글 GUI 스모크 신규 검증 경로 확보** — `_smoke_rl.py`로 전장+AI 전술 동시 ON 워커경로 자동 검증(엔진 직접호출 우회 없이). 다음 숙제: self-play 학습 .py(selfplay_env/loop)는 빌드제외라 회귀·스모크 사각 → 향후 학습 인프라 변경 시 `_selfplay_train.py` 단발 롤아웃을 audit 절차에 편입 고려.

## [2026-06-23] v15 블록 종합 감사 — 지속 전장 엔진 + RL + 다운스트림 컷오버 (트리거: 1 — 큰 묶음 완료)
범위: v15.06.01 ~ v15.13.05 (감사 시점 60커밋, 전장 엔진 블록 / engine_v7 +1004줄·launcher 대규모 리팩터·rl_env.py 신설·engine.py +129줄)
범위 주석: 「60커밋」은 ①코드·로직(diff 리뷰)에만 해당하는 경계. ②DB·③회귀·⑥위생·⑧수치·⑨누수는 현재 코드 전수라 v15.01~05(적응형 AI·위협방위 등) 엔진 상태도 이미 포괄. v15.01~05는 각자 마이너 감사 통과+전장 엔진이 그 위에 통합 구동 → 재감사 한계효용 낮아 블록 범위 유지.
방식: **무인 모드**(CLAUDE.md 종합 감사 0) — 단계별 동의 없이 자동 수행, exe 스모크는 GUI 자동화(pywinauto).
메타: 소요 빌드~4.1분·GUI스모크~35초·MC~15초(정밀 단계계측은 v16부터) · 무인 100%(개입 0회) · 재감사 0회 · 점검규모 회귀 8×26·audit_scan 11·MC n=20/8·BattleEngine 25메서드 · 환경 HEAD 8f23e45·Python 3.14.4·Win11.

| 영역 | 판정 | 발견 항목 | 조치 |
|------|------|-----------|------|
| ① 코드·로직     | **PASS** | cfg=dict(cfg) 진입부 복사 OK·전역 DB mutate 0·BattleEngine 25메서드 부모 시그니처 보존(`_apply_ship_evasion`은 호출부 `()` 무인자라 호환)·NoScrollComboBox 준수(raw QComboBox 0)·동작없는 체크박스 0·신기능8항목(3종세트 3·MC3경로·_id_counter·spec_db동기·_log/frames가드) 전부 충족 | — (병합된 블록이라 code-review 타깃 diff 부재 → 표적 수동점검+회귀로 대체) |
| ② DB·수치       | **PASS** | spec_db 107 = 엔티티DB합(ENEMY65+FRIENDLY14+SHIP21+AIRCRAFT7) 정확일치 · SHIP_ENDURANCE/PROCUREMENT 21=SHIP_DB21 · 공개제원 스팟체크 일치(J-16=6·055형=24·야센=32·랴오닝 $3B·022형 $40M) · surrogate table 345=15편대×23적 누락0·전부 프리셋 일치 | — |
| ③ 회귀          | **PASS** | 8케이스×26지표 골든 일치(동작 보존) · 결정론 유지(신규 RNG가 단발 경로 안 건드림) | — |
| ④ 통합 MC+성능  | **PASS** | 단발MC 요격률 0.749±0.078(전면전 포화) · 전장MC 승률 0.375·임무점수 0.552([[project-baseline-battle]] 53.2% 정합) · wall-time 단발 0.33s/전장 0.68s(이상 급증 없음) · NaN/Inf 0 | — |
| ⑤ exe·빌드      | **PASS** | 전체 빌드 exit0(exe 52MB) · 번들무결성 OK(_internal에 battle_surrogate·changelog·spec_db·engine·cesium) · **GUI 자동화 스모크 PASS**(홈→앱→시뮬실행→요격률 표시→정상종료) · 종료 후 잔존 프로세스 0(Job Object 구조 보장) | — |
| ⑥ 위생          | **PASS** | APP_VERSION=헤더=changelog 마지막 v15.13.05 정합 · gitignore 커버 완전(_rl_*·zip·token·dist/build) · 민감 산출물 추적 0 → **`audit_scan.py` 신설**(11/11 PASS) | — |
| ⑦ 하위호환      | **PASS** | v15 플래그(enable_battle_mode·munition_limit·ship_evasion·battle_*) 전부 누락한 구버전 cfg로 단발·전장 모두 무크래시·기본값 적용 | — |
| ⑧ 수치·단위     | **PASS** | 전장 분모 전부 가드(`_fr/_en_value_init`·`_ammo_init`=max(1.0,..)·`fw`/`ew`=or 1.0·`_init_dist`>0 가드) · 승리당비용 win_rate=0 가드(`if wr and wr>0`) · chan>0 가드 · MC NaN/Inf 0 | — |
| ⑨ 리소스 누수   | **PASS** | `_log()` 내부 `if self._mc_mode: return` · `_record_frame` 호출 `if not _mc_mode` 가드 · matplotlib figure 생성12/close25+clear 패턴 · 과거 BUG-1(fig.clf 누락) 기수정 | — |

종합 판정: **통과** (9영역 전부 PASS, 발견 0건)
요약: v15 지속 전장 엔진·RL·다운스트림 컷오버 블록 무인 종합 감사 — 신기능 체크리스트·부모 무수정·전장 분모 가드·하위호환·GUI 스모크 전부 클린. `audit_scan.py`(정적 스캐너 11항목)·`_audit_smoke.py`(GUI 자동화 스모크) 신설.
메타 회고: 약점 3건 식별 → ▸병합 후 `/code-review` 빈 diff(표적 수동 Grep+회귀로 대체, 에이전트 의미추적 미수행 — 다음엔 누적 diff 직접 투입 또는 shift-left) ▸abort 클릭 미검증(Job Object+잔존0로 대체 — `_audit_smoke.py`에 추가 예정) ▸surrogate/DB 키 구조 오가정 헛돔(audit_scan 자동 정합으로 최소화 완료). 루프 제도화: CLAUDE.md '종합 감사 6) 메타 회고' + [[feedback-audit-self-improve]].
